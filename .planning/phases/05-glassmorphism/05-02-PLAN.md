---
phase: 05-glassmorphism
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - Views/Shared/_Layout.cshtml
  - Views/Home/Index.cshtml
autonomous: true
requirements: [REQ-19]

must_haves:
  truths:
    - "Logout button only visible when user is authenticated"
    - "Sign-in/Sign-up buttons hidden when user is authenticated"
    - "Guest users see sign-in/sign-up but not logout"
    - "Session state is checked correctly in views"
  artifacts:
    - path: "Views/Shared/_Layout.cshtml"
      provides: "Session-aware conditional button rendering"
      contains: "TempData"
  key_links:
    - from: "Views/Shared/_Layout.cshtml"
      to: "Controllers/UserController.cs"
      via: "Session state via TempData"
      pattern: "TempData\[\"username\"\]"
---

<objective>
Fix session-aware button visibility in the layout: show logout only when user is authenticated, hide sign-in/sign-up when authenticated.

Purpose: Currently the layout always shows all auth buttons regardless of session state. This creates a confusing UX where users see "Log in" even when already logged in, and don't see "Logout" when they need it.

Output: Conditional button rendering in _Layout.cshtml based on session state, with proper button visibility for authenticated vs guest users.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@Views/Shared/_Layout.cshtml
@Views/Home/Index.cshtml
@Controllers/UserController.cs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Session-Aware Button Visibility to Layout</name>
  <files>Views/Shared/_Layout.cshtml</files>
  <read_first>
    - Views/Shared/_Layout.cshtml (current button structure)
    - Controllers/UserController.cs (session storage pattern)
    - Views/Home/Index.cshtml (already uses TempData for conditional rendering)
  </read_first>
  <action>
Update `Views/Shared/_Layout.cshtml` to conditionally render auth buttons based on session state.

The session stores user info via `Session.Store(HttpContext, "userSession", new { UserId, Username, Email })`. The views access this via `TempData["username"]` (or check if the user object exists in TempData).

**Current pattern in Index.cshtml** (already works):
```html
@if (TempData["username"] != null)
{
    <text>Welcome, @TempData["username"]</text>
}
```

**Update _Layout.cshtml navbar buttons:**

Find the auth buttons section (sign-in, sign-up, logout) and wrap them in conditional blocks:

```html
@* Auth buttons - conditional on session state *@
@if (TempData["username"] != null)
{
    @* Authenticated user - show logout *@
    <a href="/user/log-out"
       class="inline-flex items-center gap-2 px-4 py-2 bg-red-500 text-white text-sm font-medium rounded-lg hover:bg-red-600 transition-colors">
        Log out
        <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M17 16l4-4m0 0l-4-4m4 4H7m6 4v1a3 3 0 01-3 3H6a3 3 0 01-3-3V7a3 3 0 013-3h4a3 3 0 013 3v1"/>
        </svg>
    </a>
}
else
{
    @* Guest user - show sign in/sign up *@
    <a href="/user/log-in"
       class="inline-flex items-center gap-2 px-4 py-2 text-sm font-medium text-gray-700 hover:bg-gray-100 rounded-lg transition-colors">
        Log in
    </a>
    <a href="/user/sign-up"
       class="inline-flex items-center gap-2 px-4 py-2 bg-primary text-white text-sm font-medium rounded-lg hover:bg-primary-600 transition-colors">
        Sign up
    </a>
}
```

Also update the sidebar navigation links:
- "My Templates" link: only show when authenticated
- "Create Template" link: only show when authenticated
- "Log in" link in sidebar: only show when guest

**Important:** Keep the existing mobile hamburger menu and sidebar structure intact. Only add conditional logic around the auth-specific buttons.
  </action>
  <verify>
    <automated>grep -q "TempData\[.username.\]" Views/Shared/_Layout.cshtml && grep -q "log-out" Views/Shared/_Layout.cshtml && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - _Layout.cshtml checks TempData["username"] for session state
    - Logout button only rendered when TempData["username"] is not null
    - Sign-in/Sign-up buttons only rendered when TempData["username"] is null
    - Sidebar nav links conditionally shown based on auth state
    - Existing responsive mobile menu behavior is maintained
    - dotnet build passes with 0 errors
  </acceptance_criteria>
  <done>Layout buttons conditionally rendered based on authentication state</done>
</task>

<task type="auto">
  <name>Task 2: Update Home Page Conditional Rendering</name>
  <files>Views/Home/Index.cshtml</files>
  <read_first>
    - Views/Home/Index.cshtml (current conditional rendering)
  </read_first>
  <action>
Verify and enhance the home page conditional rendering:

1. **Hero section** (guest users): Should show "Get Started" button linking to /user/sign-up
2. **User greeting** (authenticated): Should show welcome message and quick actions
3. **Search results / template grid**: Should show for all users

Check that Index.cshtml already uses `TempData["username"]` correctly for:
- Showing hero vs greeting section
- Showing appropriate CTA buttons

If any buttons need updating for consistency with the layout changes, update them now:
- "Get Started" button (guest): link to /user/sign-up
- "Create Template" button (authenticated): link to /template/create
- Ensure logout is NOT shown on home page (it's in the layout navbar)
  </action>
  <verify>
    <automated>grep -q "TempData\[.username.\]" Views/Home/Index.cshtml && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - Index.cshtml uses TempData["username"] for conditional rendering
    - Guest users see hero section with "Get Started" CTA
    - Authenticated users see greeting with "Create Template" CTA
    - No redundant auth buttons on home page (layout handles those)
    - dotnet build passes with 0 errors
  </acceptance_criteria>
  <done>Home page conditionally renders hero vs greeting based on authentication state</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Session → View | TempData read from session for conditional rendering |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-02 | Information Disclosure | Button visibility | accept | Button visibility is UI-only, no sensitive data exposed |
</threat_model>

<verification>
1. Run `dotnet build` — ensure no compilation errors
2. Start the app without logging in — verify sign-in/sign-up buttons visible, logout hidden
3. Log in — verify sign-in/sign-up hidden, logout visible
4. Check sidebar nav links change based on auth state
5. Log out — verify buttons revert to guest state
6. Check mobile responsive layout — buttons still work correctly
</verification>

<success_criteria>
- Guest users see: Log in, Sign up buttons
- Authenticated users see: Logout button only
- Sign-in/Sign-up hidden when authenticated
- Logout hidden when not authenticated
- Sidebar nav links reflect auth state
- Responsive behavior maintained
- dotnet build passes with 0 errors
</success_criteria>

<output>
Create `.planning/phases/05-glassmorphism/05-02-SUMMARY.md` when done
</output>
