---
phase: 05-glassmorphism
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - wwwroot/css/tailwind.css
  - Views/Shared/_Layout.cshtml
  - Views/Home/Index.cshtml
  - Views/User/LogInView.cshtml
  - Views/User/SignUpView.cshtml
  - Views/Template/CreateTemplate.cshtml
  - Views/Template/TemplateView.cshtml
autonomous: true
requirements: [REQ-18]

must_haves:
  truths:
    - "Glassmorphism classes are defined and available for use"
    - "Cards, sidebar, and navbar use frosted glass effect"
    - "Effects work on both light and semi-dark backgrounds"
    - "Existing responsive behavior is maintained"
  artifacts:
    - path: "wwwroot/css/tailwind.css"
      provides: "Glassmorphism utility classes"
      contains: "backdrop-blur"
  key_links:
    - from: "wwwroot/css/tailwind.css"
      to: "Views/**/*.cshtml"
      via: "Glass utility classes"
      pattern: "glass"
---

<objective>
Add glassmorphism visual effects to frontend elements for a modern, frosted-glass aesthetic.

Purpose: The current UI uses solid backgrounds and shadows. Glassmorphism adds depth and modernity with frosted-glass effects (backdrop-blur, semi-transparent backgrounds, subtle borders) that are visually appealing and follow current design trends.

Output: Glass utility classes in tailwind.css, updated layout and views with glass effects on cards, sidebar, navbar, and form containers.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@wwwroot/css/tailwind.css
@Views/Shared/_Layout.cshtml
@Views/Home/Index.cshtml
@Views/User/LogInView.cshtml
@Views/User/SignUpView.cshtml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define Glassmorphism Utility Classes</name>
  <files>wwwroot/css/tailwind.css</files>
  <read_first>
    - wwwroot/css/tailwind.css (current state)
  </read_first>
  <action>
Add glassmorphism utility classes to the `@layer components` section of `wwwroot/css/tailwind.css`:

```css
@layer components {
  /* Glassmorphism utilities */
  .glass {
    background: rgba(255, 255, 255, 0.7);
    backdrop-filter: blur(12px);
    -webkit-backdrop-filter: blur(12px);
    border: 1px solid rgba(255, 255, 255, 0.3);
  }

  .glass-strong {
    background: rgba(255, 255, 255, 0.85);
    backdrop-filter: blur(20px);
    -webkit-backdrop-filter: blur(20px);
    border: 1px solid rgba(255, 255, 255, 0.4);
  }

  .glass-subtle {
    background: rgba(255, 255, 255, 0.5);
    backdrop-filter: blur(8px);
    -webkit-backdrop-filter: blur(8px);
    border: 1px solid rgba(255, 255, 255, 0.2);
  }

  .glass-dark {
    background: rgba(17, 24, 39, 0.6);
    backdrop-filter: blur(12px);
    -webkit-backdrop-filter: blur(12px);
    border: 1px solid rgba(255, 255, 255, 0.1);
  }
}
```

Keep the existing `btn-primary`, `btn-secondary`, `card`, `input-field` classes — just add the new glass classes.
  </action>
  <verify>
    <automated>grep -q "\.glass {" wwwroot/css/tailwind.css && grep -q "backdrop-filter" wwwroot/css/tailwind.css && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - tailwind.css contains `.glass` class with backdrop-filter: blur(12px)
    - tailwind.css contains `.glass-strong` class with backdrop-filter: blur(20px)
    - tailwind.css contains `.glass-subtle` class with backdrop-filter: blur(8px)
    - tailwind.css contains `.glass-dark` class with dark background
    - Existing component classes (btn-primary, card, etc.) are preserved
  </acceptance_criteria>
  <done>Glassmorphism utility classes defined and available for use in views</done>
</task>

<task type="auto">
  <name>Task 2: Apply Glass Effects to Layout and Views</name>
  <files>Views/Shared/_Layout.cshtml, Views/Home/Index.cshtml, Views/User/LogInView.cshtml, Views/User/SignUpView.cshtml, Views/Template/CreateTemplate.cshtml, Views/Template/TemplateView.cshtml</files>
  <read_first>
    - Views/Shared/_Layout.cshtml (sidebar and navbar structure)
    - Views/Home/Index.cshtml (hero, cards, search)
    - Views/User/LogInView.cshtml (form card)
    - Views/User/SignUpView.cshtml (form card)
    - Views/Template/CreateTemplate.cshtml (editor area)
    - Views/Template/TemplateView.cshtml (response form)
  </read_first>
  <action>
Apply glassmorphism classes to key UI elements across all views:

1. **_Layout.cshtml:**
   - Sidebar: Add `glass` class (frosted glass sidebar)
   - Top navbar: Add `glass-strong` class (solid frosted nav)
   - Mobile overlay sidebar: Add `glass` class

2. **Index.cshtml (Home):**
   - Hero section (guest): Keep gradient background, no glass
   - User greeting card: Add `glass` class
   - Search input container: Add `glass-subtle` class
   - Template cards: Add `glass` class instead of `bg-white shadow-soft`
   - Skeleton cards: Add `glass-subtle` class

3. **LogInView.cshtml:**
   - Form card: Add `glass-strong` class (prominent frosted card)

4. **SignUpView.cshtml:**
   - Form card: Add `glass-strong` class (consistent with login)

5. **CreateTemplate.cshtml:**
   - Question type selector panel: Add `glass` class
   - Template editor area: Add `glass` class
   - Question cards: Add `glass-subtle` class

6. **TemplateView.cshtml:**
   - Response form container: Add `glass` class
   - Header card: Add `glass` class

**Pattern:** Replace `bg-white rounded-2xl shadow-soft` with `glass rounded-2xl` or `glass-strong rounded-2xl` as appropriate. Keep border-radius, keep existing Tailwind spacing.
  </action>
  <verify>
    <automated>grep -l "glass" Views/Shared/_Layout.cshtml Views/Home/Index.cshtml Views/User/LogInView.cshtml Views/User/SignUpView.cshtml 2>/dev/null | wc -l | xargs test 4 -le && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - _Layout.cshtml sidebar has `glass` class
    - _Layout.cshtml navbar has `glass-strong` class
    - Index.cshtml template cards have `glass` class
    - LogInView.cshtml form card has `glass-strong` class
    - SignUpView.cshtml form card has `glass-strong` class
    - CreateTemplate.cshtml editor areas have `glass` class
    - TemplateView.cshtml response form has `glass` class
    - All views maintain existing responsive behavior
    - dotnet build passes with 0 errors
  </acceptance_criteria>
  <done>Glassmorphism effects applied to layout, cards, sidebar, navbar, and form containers</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CSS → Client | Glassmorphism effects are client-side only, no security impact |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | N/A | CSS visual effects | accept | Purely cosmetic, no security implications |
</threat_model>

<verification>
1. Run `dotnet build` — ensure no compilation errors
2. Open the app in a browser — verify glass effects on sidebar, navbar, cards
3. Check that frosted glass effect is visible (semi-transparent with blur)
4. Resize to mobile — verify glass effects work on responsive layout
5. Verify no visual regressions on form inputs or buttons
</verification>

<success_criteria>
- Glass effects visible on sidebar, navbar, cards, and form containers
- Backdrop blur creates frosted glass appearance
- Semi-transparent backgrounds allow content behind to show through
- All existing functionality remains intact
- Responsive behavior maintained on all screen sizes
- dotnet build passes with 0 errors
</success_criteria>

<output>
Create `.planning/phases/05-glassmorphism/05-01-SUMMARY.md` when done
</output>
