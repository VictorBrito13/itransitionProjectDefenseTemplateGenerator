---
phase: 02-design-tokens
plan: 03
type: execute
wave: 2
depends_on: [02-01]
files_modified:
  - wwwroot/js/createTemplate/Checkbox-question.js
  - wwwroot/js/createTemplate/Multiple-options-question.js
  - wwwroot/js/createTemplate/Multiline-question.js
  - wwwroot/js/createTemplate/Single-string-question.js
  - wwwroot/js/createTemplate/Positive-integer-question.js
  - wwwroot/js/utils/buildForm.js
  - wwwroot/UI/components/btnLogOut.js
  - wwwroot/lib/bootstrap/ (remove directory)
autonomous: true
requirements: [REQ-09]

must_haves:
  truths:
    - "No JavaScript files reference Bootstrap class names (btn btn-danger, form-control, etc.)"
    - "All dynamic DOM elements created by JS use Tailwind classes"
    - "Unused Bootstrap library files are removed"
    - "All views share consistent visual styling for buttons, inputs, and interactive elements"
  artifacts:
    - path: "wwwroot/js/createTemplate/*.js"
      provides: "Template builder JS without Bootstrap classes"
      not_contains: "btn btn-|form-control"
    - path: "wwwroot/js/utils/buildForm.js"
      provides: "Build form utility without Bootstrap classes"
      not_contains: "btn btn-"
  key_links:
    - from: "wwwroot/js/createTemplate/Checkbox-question.js"
      to: "Views/Template/CreateTemplate.cshtml"
      via: "Dynamic question rendering with Tailwind classes"
      pattern: "rounded-lg|border-gray-300"
---

<objective>
Audit and fix all Bootstrap class remnants in JavaScript files, ensuring dynamic DOM elements use Tailwind classes consistently. Remove unused Bootstrap library files.

Purpose: Phase 1 migrated CSHTML views but left Bootstrap class references in 7+ JS files that dynamically create DOM elements. These create visual inconsistencies — buttons rendered with Bootstrap classes have no styling since the Bootstrap CSS is no longer loaded. This plan fixes those remnants for full visual consistency.

Output: All JS files migrated to Tailwind, Bootstrap library directory removed, consistent visual styling across dynamically created elements.
</objective>

<execution_context>
@/home/victor/.config/opencode/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-design-tokens/02-CONTEXT.md
@.planning/phases/02-design-tokens/02-01-PLAN.md
@.planning/design-tokens.md
@wwwroot/js/createTemplate/Checkbox-question.js
@wwwroot/js/createTemplate/Multiple-options-question.js
@wwwroot/js/createTemplate/Multiline-question.js
@wwwroot/js/createTemplate/Single-string-question.js
@wwwroot/js/createTemplate/Positive-integer-question.js
@wwwroot/js/utils/buildForm.js
@wwwroot/UI/components/btnLogOut.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix Bootstrap Class Remnants in Question-Type JS Files</name>
  <files>
    wwwroot/js/createTemplate/Checkbox-question.js,
    wwwroot/js/createTemplate/Multiple-options-question.js,
    wwwroot/js/createTemplate/Multiline-question.js,
    wwwroot/js/createTemplate/Single-string-question.js,
    wwwroot/js/createTemplate/Positive-integer-question.js
  </files>
  <read_first>
    - wwwroot/js/createTemplate/Checkbox-question.js (has `btn btn-danger`)
    - wwwroot/js/createTemplate/Multiple-options-question.js (has `btn btn-primary`, `btn btn-danger`)
    - wwwroot/js/createTemplate/Multiline-question.js (has `form-control`, `btn btn-danger`)
    - wwwroot/js/createTemplate/Single-string-question.js (has `form-control`, `btn btn-danger`)
    - wwwroot/js/createTemplate/Positive-integer-question.js (has `form-control`, `btn btn-danger`)
    - .planning/design-tokens.md (reference Tailwind class patterns)
    - Views/Template/CreateTemplate.cshtml (reference existing Tailwind button patterns)
  </read_first>
  <action>
Replace ALL Bootstrap class references in each question-type JS file with Tailwind equivalents.

**Target replacement patterns (apply to ALL 5 files):**

| Bootstrap Class | Tailwind Replacement |
|----------------|---------------------|
| `btn btn-danger` | `px-3 py-1.5 bg-red-500 text-white text-sm font-medium rounded-lg hover:bg-red-600 focus:ring-2 focus:ring-red-500 transition-colors` |
| `btn btn-primary` | `px-3 py-1.5 bg-primary text-white text-sm font-medium rounded-lg hover:bg-primary-600 focus:ring-2 focus:ring-primary transition-colors` |
| `form-control` | `w-full px-3 py-2 border border-gray-300 rounded-lg focus:ring-2 focus:ring-primary focus:border-transparent transition-all text-sm` |
| `form-select` | `w-full px-3 py-2 border border-gray-300 rounded-lg focus:ring-2 focus:ring-primary focus:border-transparent transition-all text-sm bg-white` |

**Per-file specific changes:**

1. **Checkbox-question.js** (line 34):
   - Replace `$btnDeleteQuestion.className = "btn btn-danger";`
   - With: `$btnDeleteQuestion.className = "px-3 py-1.5 bg-red-500 text-white text-sm font-medium rounded-lg hover:bg-red-600 focus:ring-2 focus:ring-red-500 transition-colors";`

2. **Multiple-options-question.js** (lines 54, 72, 83):
   - Line 54: Replace `$btnEditOptions.className = "btn btn-primary";`
     - With: `$btnEditOptions.className = "px-3 py-1.5 bg-primary text-white text-sm font-medium rounded-lg hover:bg-primary-600 focus:ring-2 focus:ring-primary transition-colors";`
   - Line 72: Replace `$btnDeleteOption.className = "btn btn-danger";` with `$btnDeleteOption.className = "px-3 py-1 bg-red-500 text-white text-xs font-medium rounded-lg hover:bg-red-600 transition-colors";`
   - Line 83: Replace `$btnDeleteQuestion.className = "btn btn-danger";` with same Tailwind dangerous button pattern

3. **Multiline-question.js** (lines 19, 33):
   - Line 19: Replace `$textarea.classList.add("form-control");` with `$textarea.classList.add(...)"w-full px-3 py-2 border border-gray-300 rounded-lg focus:ring-2 focus:ring-primary focus:border-transparent transition-all text-sm".split(" "));`
   - Line 33: Replace `$btnDeleteQuestion.className = "btn btn-danger";` with Tailwind danger button

4. **Single-string-question.js** (lines 17, 32):
   - Line 17: Replace `$input.className = "form-control";` with `$input.className = "w-full px-3 py-2 border border-gray-300 rounded-lg focus:ring-2 focus:ring-primary focus:border-transparent transition-all text-sm";`
   - Line 32: Replace `$btnDeleteQuestion.className = "btn btn-danger";` with Tailwind danger button

5. **Positive-integer-question.js** (lines 17, 34):
   - Line 17: Replace `$input.className = "form-control";` with `$input.className = "w-full px-3 py-2 border border-gray-300 rounded-lg focus:ring-2 focus:ring-primary focus:border-transparent transition-all text-sm";`
   - Line 34: Replace `$btnDeleteQuestion.className = "btn btn-danger";` with Tailwind danger button

6. **CreateTemplate.cshtml views:** Check for any remaining `id="form-control-search-user"` — this is an ID not a class so it's fine, but verify there are no other Bootstrap patterns.

**IMPORTANT — Tailwind classList.add vs className:**
- When the JS uses `.className = "..."`, replace the entire string with Tailwind classes
- When the JS uses `.classList.add("...")`, use `.classList.add(...TAILWIND_CLASSES.split(" "))`
- When the JS uses `$element.className = "..."` (direct assignment), replace the entire string

After modifications, verify each file has NO Bootstrap class strings by searching for `btn btn-`, `form-control`, `form-select`, `alert`, `badge`, `text-danger`, `text-success`, `bg-danger`, `bg-success`, `text-light` in the modified files.
  </action>
  <verify>
    <automated>
      for f in wwwroot/js/createTemplate/Checkbox-question.js wwwroot/js/createTemplate/Multiple-options-question.js wwwroot/js/createTemplate/Multiline-question.js wwwroot/js/createTemplate/Single-string-question.js wwwroot/js/createTemplate/Positive-integer-question.js; do
        if grep -q 'btn btn-\|"form-control"\|"form-select"' "$f" 2>/dev/null; then echo "FAIL: $f has Bootstrap remnants"; exit 1; fi
      done
      echo "PASS"
    </automated>
  </verify>
  <acceptance_criteria>
    - Checkbox-question.js does NOT contain "btn btn-danger"
    - Multiple-options-question.js does NOT contain "btn btn-primary" or "btn btn-danger"
    - Multiline-question.js does NOT contain "form-control" or "btn btn-danger"
    - Single-string-question.js does NOT contain "form-control" or "btn btn-danger"
    - Positive-integer-question.js does NOT contain "form-control" or "btn btn-danger"
    - All files contain Tailwind classes (e.g., "rounded-lg", "bg-red-500", "border-gray-300")
    - dotnet build passes with 0 errors
  </acceptance_criteria>
  <done>All 5 question-type JS files migrated from Bootstrap to Tailwind classes</done>
</task>

<task type="auto">
  <name>Task 2: Fix Bootstrap Remnants in buildForm.js and btnLogOut.js</name>
  <files>wwwroot/js/utils/buildForm.js, wwwroot/UI/components/btnLogOut.js</files>
  <read_first>
    - wwwroot/js/utils/buildForm.js (has `btn btn-success`)
    - wwwroot/UI/components/btnLogOut.js (has `btn btn-danger` and `bi bi-box-arrow-right` icon)
  </read_first>
  <action>
1. **wwwroot/js/utils/buildForm.js** (line 53):
   - Replace `$btnSubmit.className = "btn btn-success"`
   - With: `$btnSubmit.className = "px-8 py-3 bg-secondary text-white font-medium rounded-xl hover:bg-secondary-600 focus:ring-2 focus:ring-secondary focus:ring-offset-2 transition-colors";`
   
   This uses the secondary (green) color which matches the success semantic of `btn btn-success`.
   
   Also check if any other Bootstrap class strings exist in this file (e.g., `form-control`, `btn`, `alert`). Search for any remaining Bootstrap patterns:
   ```javascript
   const bootstrapPattern = /(btn btn-|form-control|form-select|alert |text-danger|text-success|badge|table-)/;
   // If found, replace with appropriate Tailwind equivalent
   ```

2. **wwwroot/UI/components/btnLogOut.js** (line 7):
   - Replace `$a.className = "btn btn-danger";`
   - With: `$a.className = "inline-flex items-center gap-2 px-4 py-2 bg-red-500 text-white text-sm font-medium rounded-lg hover:bg-red-600 focus:ring-2 focus:ring-red-500 transition-colors";`
   - Replace `$a.innerHTML = ...` line:
     - Remove the Bootstrap icon `bi bi-box-arrow-right` 
     - Replace with inline SVG or use simpler text + arrow unicode:
     ```javascript
     $a.innerHTML = 'Log out <svg class="w-4 h-4 inline" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M17 16l4-4m0 0l-4-4m4 4H7m6 4v1a3 3 0 01-3 3H6a3 3 0 01-3-3V7a3 3 0 013-3h4a3 3 0 013 3v1"/></svg>';
     ```
  </action>
  <verify>
    <automated>
      if grep -q 'btn btn-\|"form-control"' wwwroot/js/utils/buildForm.js 2>/dev/null; then echo "FAIL: buildForm.js has Bootstrap remnants"; exit 1; fi
      if grep -q 'btn btn-' wwwroot/UI/components/btnLogOut.js 2>/dev/null; then echo "FAIL: btnLogOut.js has Bootstrap remnants"; exit 1; fi
      if grep -q 'bi bi-' wwwroot/UI/components/btnLogOut.js 2>/dev/null; then echo "FAIL: btnLogOut.js has Bootstrap icons"; exit 1; fi
      echo "PASS"
    </automated>
  </verify>
  <acceptance_criteria>
    - wwwroot/js/utils/buildForm.js does NOT contain "btn btn-success"
    - wwwroot/js/utils/buildForm.js uses "bg-secondary" Tailwind classes
    - wwwroot/UI/components/btnLogOut.js does NOT contain "btn btn-danger"
    - wwwroot/UI/components/btnLogOut.js does NOT contain "bi bi-" (Bootstrap icons)
    - wwwroot/UI/components/btnLogOut.js contains inline SVG for logout icon
    - dotnet build passes with 0 errors
  </acceptance_criteria>
  <done>buildForm.js and btnLogOut.js migrated from Bootstrap to Tailwind classes</done>
</task>

<task type="auto">
  <name>Task 3: Remove Unused Bootstrap Library Directory</name>
  <files>wwwroot/lib/bootstrap/ (remove directory)</files>
  <read_first>
    - package.json (check if bootstrap is listed as a dependency)
    - Views/Shared/_Layout.cshtml (verify no Bootstrap references remain)
  </read_first>
  <action>
1. **Verify no Bootstrap references remain** — search all CSHTML files for bootstrap[min] references:
   ```bash
   grep -r "bootstrap" Views/ --include="*.cshtml" | grep -v "node_modules" || echo "No bootstrap references in views"
   ```

2. **Remove the Bootstrap library directory** only if confirmed unused:
   ```bash
   rm -rf wwwroot/lib/bootstrap/
   ```
   
   **Before removing:** Double-check that `wwwroot/lib/bootstrap/` is not referenced by any view. Phase 1 (Plan 01-SUMMARY.md) confirmed Bootstrap CDN links were removed from _Layout.cshtml. The `wwwroot/lib/bootstrap/` directory is leftover from the original project template.
   
   **Note:** The `wwwroot/lib/jquery/` directory should be kept since jQuery is still used by the application for validation and other functionality.

3. **Update .gitignore** (if needed): Add `wwwroot/lib/` pattern if the entire lib directory should be excluded or add an exception:
   ```
   # Keep only essential libs (jQuery)
   wwwroot/lib/*
   !wwwroot/lib/jquery/
   ```
   
   Actually, since jQuery is still used, only remove the Bootstrap subdirectory. Do NOT modify .gitignore.

4. **Document the removal** — the SUMMARY.md should note that Bootstrap JS/CSS library files have been removed since they're no longer referenced. jQuery lib files remain.
  </action>
  <verify>
    <automated>test ! -d wwwroot/lib/bootstrap/ && echo "PASS: Bootstrap directory removed" || echo "FAIL: Bootstrap directory still exists"</automated>
  </verify>
  <acceptance_criteria>
    - wwwroot/lib/bootstrap/ directory does not exist
    - wwwroot/lib/jquery/ directory still exists (jQuery is still used)
    - No compilation or runtime errors after removal
    - Bootstrap CSS/JS not referenced in any .cshtml view (cross-check with grep)
  </acceptance_criteria>
  <done>Unused Bootstrap library directory removed — jQuery library kept (still in use)</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| N/A | No new trust boundaries introduced — changes are styling-only in existing JS files |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-05 | Tampering | Legacy Bootstrap lib | accept | Lib directory removal is safe; only unused files deleted |
| T-02-06 | Denial of Service | JS class changes | accept | Visual-only changes, no functional impact on form logic |
</threat_model>

<verification>
1. Run `dotnet build` — ensure no compilation errors
2. Test template builder: create a new template, add each question type — verify delete buttons render with Tailwind styling
3. Test multiple options question: add options, edit options — verify buttons have Tailwind styling
4. Test template response: fill and submit a form — verify submit button has Tailwind "secondary" styling
5. Verify logout button in the UI renders correctly with Tailwind classes
6. Run the app and verify no 404 errors for Bootstrap JS/CSS (they should not be requested)
7. Verify Bootstrap lib directory is removed
</verification>

<success_criteria>
- All 7 JS files have Bootstrap classes replaced with Tailwind equivalents
- Bootstrap library directory (`wwwroot/lib/bootstrap/`) removed
- Template builder question type buttons render with correct Tailwind styling
- Logout button renders with danger (red) styling using Tailwind
- Submit buttons use Tailwind secondary (green) styling
- No 404 errors for Bootstrap resources
- `dotnet build` passes with 0 errors
- All form functionality remains intact (questions can be added/deleted, forms submitted)
</success_criteria>

<output>
Create `.planning/phases/02-design-tokens/02-03-SUMMARY.md` when done
</output>
