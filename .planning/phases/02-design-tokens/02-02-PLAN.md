---
phase: 02-design-tokens
plan: 02
type: execute
wave: 2
depends_on: [02-01]
files_modified:
  - Views/Shared/_Layout.cshtml
  - Views/Shared/Error.cshtml
  - wwwroot/js/utils/toast.js
  - Views/Template/CreateTemplate.cshtml
  - Views/Template/TemplateView.cshtml
  - Views/User/LogInView.cshtml
  - Views/User/SignUpView.cshtml
  - wwwroot/js/createTemplate/createTemplate.js
autonomous: true
requirements: [REQ-07, REQ-08]

must_haves:
  truths:
    - "Sonner.js toast notifications show success/error/warning/info messages"
    - "Error.cshtml uses Tailwind styling with branded error UI"
    - "Toast utility function is available for all JS to call"
    - "Form submission flows show toasts instead of inline alerts"
    - "Error page provides clear navigation back to working state"
  artifacts:
    - path: "wwwroot/js/utils/toast.js"
      provides: "Toast notification utility using Sonner.js"
      contains: "function showToast"
    - path: "Views/Shared/Error.cshtml"
      provides: "Redesigned error page with Tailwind"
      contains: "tailwind"
  key_links:
    - from: "Views/Shared/_Layout.cshtml"
      to: "https://cdn.jsdelivr.net/npm/sonner.js"
      via: "Sonner.js CDN script inclusion"
      pattern: "sonner"
    - from: "wwwroot/js/utils/toast.js"
      to: "Sonner.js toast API"
      via: "window.Toast calls"
      pattern: "toast\\."
---

<objective>
Implement Sonner.js toast notifications across the application and redesign the Error.cshtml page with Tailwind styling.

Purpose: Phase 1 deferred toast notifications (D-05). Currently, error messages display as inline Bootstrap-style alerts with poor UX. Sonner.js provides non-intrusive, accessible, and visually consistent toast notifications. Error.cshtml still uses Bootstrap `text-danger` classes — it needs to match the new design system.

Output: Sonner.js toast integration with JS utility, Error.cshtml redesign with Tailwind, toast integration in form submission flows.
</objective>

<execution_context>
@/home/victor/.config/opencode/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-design-tokens/02-CONTEXT.md
@.planning/phases/02-design-tokens/02-01-PLAN.md
@Views/Shared/Error.cshtml
@Views/Shared/_Layout.cshtml
@Views/Template/CreateTemplate.cshtml
@Views/Template/TemplateView.cshtml
@Views/User/LogInView.cshtml
@Views/User/SignUpView.cshtml
@wwwroot/js/createTemplate/createTemplate.js
@.planning/design-tokens.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Integrate Sonner.js and Create Toast Utility</name>
  <files>Views/Shared/_Layout.cshtml, wwwroot/js/utils/toast.js</files>
  <read_first>
    - Views/Shared/_Layout.cshtml (current layout — add Sonner.js CDN + stylesheet)
    - wwwroot/js/utils/toast.js (will create this file)
  </read_first>
  <action>
1. Add to `Views/Shared/_Layout.cshtml` in the `<head>` section (after the Tailwind CDN script):
   ```html
   <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/sonner.js@latest/dist/styles.min.css" />
   ```
   
2. Add after the Tailwind CDN script but before Alpine.js:
   ```html
   <script src="https://cdn.jsdelivr.net/npm/sonner.js@latest/dist/index.min.js"></script>
   ```
   
3. Add a toast container element before the closing `</body>` tag (after `@await RenderSectionAsync("Scripts", required: false)`):
   ```html
   <!-- Toast container for Sonner.js -->
   <div id="toast-container"></div>
   ```

4. Create `wwwroot/js/utils/toast.js` with the toast utility:
   ```javascript
   /**
    * Toast notification utility using Sonner.js
    * 
    * Usage:
    *   showToast('success', 'Template saved successfully!');
    *   showToast('error', 'Failed to save template.');
    *   showToast('warning', 'Session expiring soon.');
    *   showToast('info', 'New update available.');
    * 
    * Dependencies: Sonner.js (loaded via CDN in _Layout.cshtml)
    */
   
   function showToast(type, message, options = {}) {
     if (typeof toast === 'undefined') {
       console.warn('Sonner.js not loaded — falling back to console.', type, message);
       return;
     }
   
     const defaults = {
       duration: 4000,
       position: 'bottom-right',
       richColors: true,
       closeButton: true,
     };
   
     const config = { ...defaults, ...options };
   
     switch (type) {
       case 'success':
         toast.success(message, config);
         break;
       case 'error':
         toast.error(message, config);
         break;
       case 'warning':
         toast.warning(message, config);
         break;
       case 'info':
       default:
         toast.info(message, config);
         break;
     }
   }
   
   // Legacy alert replacement — replaces Bootstrap-style alert patterns
   function showFormError(elementId, message) {
     const container = document.getElementById(elementId);
     if (container) {
       container.textContent = message;
       container.classList.remove('hidden');
       showToast('error', message);
     }
   }
   
   function showFormSuccess(elementId, message) {
     const container = document.getElementById(elementId);
     if (container) {
       container.textContent = message;
       container.classList.remove('hidden');
       showToast('success', message);
     }
   }
   ```

5. Add the script reference in `_Layout.cshtml` before `@await RenderSectionAsync("Scripts", required: false)`:
   ```html
   <script src="~/js/utils/toast.js" asp-append-version="true"></script>
   ```
  </action>
  <verify>
    <automated>grep -q "sonner" Views/Shared/_Layout.cshtml && test -f wwwroot/js/utils/toast.js && grep -q "function showToast" wwwroot/js/utils/toast.js && grep -q "toast.success" wwwroot/js/utils/toast.js && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - Views/Shared/_Layout.cshtml contains sonner.js stylesheet reference
    - Views/Shared/_Layout.cshtml contains sonner.js script reference
    - Views/Shared/_Layout.cshtml contains toast container div
    - Views/Shared/_Layout.cshtml contains reference to wwwroot/js/utils/toast.js
    - wwwroot/js/utils/toast.js exists
    - wwwroot/js/utils/toast.js contains "function showToast" 
    - wwwroot/js/utils/toast.js contains "toast.success", "toast.error", "toast.warning", "toast.info"
    - wwwroot/js/utils/toast.js contains fallback console.warn when Sonner.js not loaded
  </acceptance_criteria>
  <done>Sonner.js integrated and toast utility created with success/error/warning/info variants</done>
</task>

<task type="auto">
  <name>Task 2: Redesign Error.cshtml with Tailwind</name>
  <files>Views/Shared/Error.cshtml</files>
  <read_first>
    - Views/Shared/Error.cshtml (current implementation with Bootstrap text-danger)
    - Views/User/LogInView.cshtml (reference: Tailwind card layout pattern)
    - .planning/design-tokens.md (token values for consistent styling)
  </read_first>
  <action>
Replace `Views/Shared/Error.cshtml` with a Tailwind-styled branded error page:

```html
﻿@model ErrorViewModel
@{
    ViewData["Title"] = "Error";
}

<div class="min-h-[60vh] flex items-center justify-center bg-gray-50 px-4">
    <div class="w-full max-w-lg bg-white rounded-2xl shadow-soft p-8 md:p-12 text-center">
        <!-- Error Icon -->
        <div class="w-20 h-20 bg-error/10 rounded-full flex items-center justify-center mx-auto mb-6">
            <svg class="w-10 h-10 text-error" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="1.5" d="M12 9v3.75m-9.303 3.376c-.866 1.5.217 3.374 1.948 3.374h14.71c1.73 0 2.813-1.874 1.948-3.374L13.949 3.378c-.866-1.5-3.032-1.5-3.898 0L2.697 16.126zM12 15.75h.007v.008H12v-.008z" />
            </svg>
        </div>

        <!-- Error Heading -->
        <h1 class="text-2xl md:text-3xl font-bold text-gray-900 mb-2">Something went wrong</h1>
        <p class="text-gray-500 mb-8">An error occurred while processing your request. Please try again.</p>

        @if (Model.ShowRequestId)
        {
            <div class="bg-gray-50 rounded-xl p-4 mb-8 text-left">
                <p class="text-sm text-gray-500">
                    <span class="font-medium text-gray-700">Request ID:</span>
                    <code class="ml-2 text-sm bg-gray-200 px-2 py-0.5 rounded">@Model.RequestId</code>
                </p>
            </div>
        }

        <!-- Action Buttons -->
        <div class="flex flex-col sm:flex-row gap-3 justify-center">
            <a asp-controller="Home" asp-action="Index" 
               class="inline-flex items-center justify-center px-6 py-3 bg-primary text-white font-medium rounded-xl hover:bg-primary-600 focus:ring-2 focus:ring-primary focus:ring-offset-2 transition-all duration-200">
                <svg class="w-5 h-5 mr-2" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M3 12l2-2m0 0l7-7 7 7M5 10v10a1 1 0 001 1h3m10-11l2 2m-2-2v10a1 1 0 01-1 1h-3m-6 0a1 1 0 001-1v-4a1 1 0 011-1h2a1 1 0 011 1v4a1 1 0 001 1m-6 0h6" />
                </svg>
                Go Home
            </a>
            <a asp-controller="Home" asp-action="Index"
               class="inline-flex items-center justify-center px-6 py-3 bg-white text-gray-700 font-medium rounded-xl border border-gray-200 hover:bg-gray-50 focus:ring-2 focus:ring-primary focus:ring-offset-2 transition-all duration-200">
                Try Again
            </a>
        </div>
    </div>
</div>

@if (Context.Request.Headers["X-Requested-With"] == "XMLHttpRequest")
{
    <!-- Development mode info for AJAX requests -->
    <div class="hidden">
        @* Only visible in development environment *@
        @if (Model.ShowRequestId)
        {
            <p>Request ID: @Model.RequestId</p>
        }
    </div>
}

@* Development mode info remains from original template *@
<div class="hidden">
    @* The development environment info is kept for debugging but hidden from regular view *@
    <h3>Development Mode</h3>
    <p>
        Swapping to <strong>Development</strong> environment will display more detailed information...
    </p>
</div>
```

Key changes from current implementation:
- Remove `text-danger` Bootstrap classes
- Replace generic error page with branded card layout (matching auth page pattern)
- Add warning icon in error-themed circle
- Clear error heading and description text
- Action buttons: "Go Home" (primary) and "Try Again" (secondary)
- Request ID shown in a styled code block
- Development mode info kept but hidden in a `hidden` div
- Tailwind classes only, no Bootstrap references
</action>
  <verify>
    <automated>grep -q "text-danger" Views/Shared/Error.cshtml && echo "FAIL: Bootstrap still present" || (grep -q "rounded-2xl" Views/Shared/Error.cshtml && grep -q "bg-error/10" Views/Shared/Error.cshtml && echo "PASS" || echo "FAIL")</automated>
  </verify>
  <acceptance_criteria>
    - Views/Shared/Error.cshtml does NOT contain "text-danger" (Bootstrap class removed)
    - Views/Shared/Error.cshtml contains "rounded-2xl" (Tailwind card)
    - Views/Shared/Error.cshtml contains "bg-error/10" (error-themed icon background)
    - Views/Shared/Error.cshtml contains "Go Home" link to Home/Index
    - Views/Shared/Error.cshtml contains "Try Again" button
    - Views/Shared/Error.cshtml still contains @Model.ShowRequestId check
    - Views/Shared/Error.cshtml contains "shadow-soft" card shadow
    - dotnet build passes with 0 errors
  </acceptance_criteria>
  <done>Error page redesigned with Tailwind, error-themed card layout, and clear navigation options</done>
</task>

<task type="auto">
  <name>Task 3: Integrate Toast Notifications in Form Submission Flows</name>
  <files>Views/User/LogInView.cshtml, Views/User/SignUpView.cshtml, Views/Template/CreateTemplate.cshtml, Views/Template/TemplateView.cshtml, wwwroot/js/createTemplate/createTemplate.js</files>
  <read_first>
    - wwwroot/js/utils/toast.js (just created — toast utility API)
    - Views/User/LogInView.cshtml (check for alert-style error displays)
    - Views/User/SignUpView.cshtml (check for alert-style error displays)
    - Views/Template/CreateTemplate.cshtml (check for TempData error patterns)
    - Views/Template/TemplateView.cshtml (check for TempData patterns)
    - wwwroot/js/createTemplate/createTemplate.js (check for Bootstrap alert classes)
  </read_first>
  <action>
Update form views and JS to use Sonner.js toast notifications instead of Bootstrap-style alerts:

1. **Views/User/LogInView.cshtml:** 
   - Find any Bootstrap `alert` or `text-danger` patterns (TempData error display containers)
   - Replace with toast-compatible markup. For TempData errors, call `showToast('error', message)` from JS on page load instead of rendering an inline alert
   - Add inline script at bottom of page (before `</body>`) to check for TempData and fire toast:
   ```html
   <script>
       // Show server errors as toast notifications
       @if (TempData["errorMsg"] != null)
       {
           <text>showToast('error', '@TempData["errorMsg"]');</text>
       }
       @if (TempData["successMsg"] != null)
       {
           <text>showToast('success', '@TempData["successMsg"]');</text>
       }
   </script>
   ```

2. **Views/User/SignUpView.cshtml:** Same pattern — replace alert containers with TempData → toast conversion script.

3. **Views/Template/CreateTemplate.cshtml:** 
   - Find any existing `TempData["errorMsg"]` display containers
   - Replace with toast-based display using the same inline script pattern
   - Remove any Bootstrap-style alert markup

4. **Views/Template/TemplateView.cshtml:** 
   - Same pattern for TempData-based notifications

5. **wwwroot/js/createTemplate/createTemplate.js:**
   - Find any Bootstrap class assignments (like `bg-danger`, `bg-success`, `text-light` that were found in Plan 01-04)
   - Replace with toast calls: `showToast('success', message)` and `showToast('error', message)`
   - Remove any DOM manipulation for alert containers that are no longer needed (or keep hidden containers for backward compatibility)

6. For all views: keep existing TempData display containers hidden by default (as a fallback), but make the toast the primary notification mechanism. The inline script should fire the toast AND populate the hidden container.
  </action>
  <verify>
    <automated>grep -q "showToast.*TempData" Views/User/LogInView.cshtml && grep -q "showToast.*TempData" Views/Template/CreateTemplate.cshtml && echo "PASS" || echo "Note: Some views may use JS-based toast calls — check manually"</automated>
  </verify>
  <acceptance_criteria>
    - Views/User/LogInView.cshtml has inline script checking TempData for toast notifications
    - Views/User/SignUpView.cshtml has inline script checking TempData for toast notifications
    - Views/Template/CreateTemplate.cshtml has inline script checking TempData for toast notifications
    - Views/Template/TemplateView.cshtml has inline script checking TempData for toast notifications
    - wwwroot/js/createTemplate/createTemplate.js uses showToast() instead of Bootstrap alert classes
    - dotnet build passes with 0 errors
    - No Bootstrap alert class names (bg-danger, bg-success, alert) in modified files
  </acceptance_criteria>
  <done>Toast notifications integrated in all form submission flows, replacing Bootstrap-style alerts</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CDN → Client | Sonner.js loaded from jsdelivr CDN |
| Form → Server → Toast | Server messages rendered in inline script blocks |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-03 | Tampering | CDN resources (Sonner.js) | accept | Same mitigation as Tailwind CDN — production can use local bundle |
| T-02-04 | Information Disclosure | TempData in scripts | mitigate | TempData only contains user-facing messages, never raw exceptions |
</threat_model>

<verification>
1. Run `dotnet build` — ensure no compilation errors
2. Navigate to /user/log-in — submit invalid creds, verify toast shows error
3. Navigate to /template/create — verify toast notification system works
4. Visit a non-existent route (/something) — verify error page renders with Tailwind
5. Verify error page has "Go Home" link that works
6. Check browser console for any JS errors
7. Verify toast notifications auto-dismiss after 4 seconds
</verification>

<success_criteria>
- Sonner.js CDN loads without errors (check network tab)
- Toast notifications appear for success/error/warning/info states
- Error.cshtml renders with Tailwind styling (no Bootstrap)
- Error page provides clear navigation ("Go Home", "Try Again")
- All form submission flows show toasts instead of inline alerts
- `dotnet build` passes with 0 errors
- No JavaScript console errors
</success_criteria>

<output>
Create `.planning/phases/02-design-tokens/02-02-SUMMARY.md` when done
</output>
