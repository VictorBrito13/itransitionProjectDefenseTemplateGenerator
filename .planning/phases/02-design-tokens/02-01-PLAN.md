---
phase: 02-design-tokens
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - wwwroot/css/tailwind.css
  - tailwind.config.js
  - .planning/design-tokens.md
autonomous: true
requirements: [REQ-06]

must_haves:
  truths:
    - "Design tokens are defined as CSS custom properties in tailwind.css"
    - "Tailwind config extends to reference the same token values"
    - "Token documentation exists as a single source of truth"
    - "All views can reference consistent spacing, typography, and color via tokens"
  artifacts:
    - path: "wwwroot/css/tailwind.css"
      provides: "Design tokens as CSS custom properties + Tailwind directives"
      contains: ":root {"
    - path: "tailwind.config.js"
      provides: "Extended Tailwind configuration using token values"
      contains: "theme.extend"
    - path: ".planning/design-tokens.md"
      provides: "Design token documentation"
      contains: "## Color Tokens"
  key_links:
    - from: "wwwroot/css/tailwind.css"
      to: "Views/**/*.cshtml"
      via: "CSS custom properties usage"
      pattern: "var(--"
    - from: "tailwind.config.js"
      to: "wwwroot/css/tailwind.css"
      via: "Shared token values"
      pattern: "var\\(--"
---

<objective>
Create a comprehensive design token system for cross-app visual consistency.
Define tokens as CSS custom properties, extend Tailwind config to use them, and document all tokens in a single source of truth.

Purpose: Currently design values (colors, spacing, shadows) are scattered between tailwind.config.js and individual views. A centralized token system ensures visual consistency, makes future theming possible, and reduces decision fatigue when building new components.

Output: tailwind.css with :root custom properties, extended tailwind.config.js, design-tokens.md documentation.
</objective>

<execution_context>
@/home/victor/.config/opencode/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-design-tokens/02-CONTEXT.md
@.planning/phases/01-ui-redesign/01-01-SUMMARY.md
@.planning/phases/01-ui-redesign/01-02-SUMMARY.md
@.planning/phases/01-ui-redesign/01-03-SUMMARY.md
@.planning/phases/01-ui-redesign/01-04-SUMMARY.md
@wwwroot/css/tailwind.css
@tailwind.config.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define CSS Custom Property Tokens in tailwind.css</name>
  <files>wwwroot/css/tailwind.css</files>
  <read_first>
    - wwwroot/css/tailwind.css (current state — replace content)
    - tailwind.config.js (reference existing theme values)
  </read_first>
  <action>
Replace `wwwroot/css/tailwind.css` with a comprehensive token system.

Structure — must keep `@tailwind base`, `@tailwind components`, `@tailwind utilities` directives AND add the token layer:

```css
@tailwind base;
@tailwind components;
@tailwind utilities;

@layer base {
  :root {
    /* ─── Color Tokens ─── */
    /* Primary */
    --color-primary-50: #EFF6FF;
    --color-primary-100: #DBEAFE;
    --color-primary-200: #BFDBFE;
    --color-primary-300: #93C5FD;
    --color-primary-400: #60A5FA;
    --color-primary-500: #3B82F6;
    --color-primary-600: #2563EB;
    --color-primary-700: #1D4ED8;
    
    /* Secondary (Emerald) */
    --color-secondary-50: #ECFDF5;
    --color-secondary-100: #D1FAE5;
    --color-secondary-200: #A7F3D0;
    --color-secondary-400: #34D399;
    --color-secondary-500: #10B981;
    --color-secondary-600: #059669;
    --color-secondary-700: #047857;
    
    /* Error (Red) */
    --color-error-50: #FEF2F2;
    --color-error-100: #FEE2E2;
    --color-error-400: #F87171;
    --color-error-500: #EF4444;
    --color-error-600: #DC2626;
    
    /* Warning (Amber) */
    --color-warning-50: #FFFBEB;
    --color-warning-100: #FEF3C7;
    --color-warning-400: #FBBF24;
    --color-warning-500: #F59E0B;
    --color-warning-600: #D97706;
    
    /* Neutral / Gray */
    --color-gray-50: #F9FAFB;
    --color-gray-100: #F3F4F6;
    --color-gray-200: #E5E7EB;
    --color-gray-300: #D1D5DB;
    --color-gray-400: #9CA3AF;
    --color-gray-500: #6B7280;
    --color-gray-600: #4B5563;
    --color-gray-700: #374151;
    --color-gray-800: #1F2937;
    --color-gray-900: #111827;
    
    /* ─── Semantic Token Aliases ─── */
    --color-bg-primary: #FFFFFF;
    --color-bg-secondary: #F9FAFB;
    --color-bg-tertiary: #F3F4F6;
    --color-text-primary: #111827;
    --color-text-secondary: #6B7280;
    --color-text-tertiary: #9CA3AF;
    --color-text-inverse: #FFFFFF;
    --color-border-default: #E5E7EB;
    --color-border-hover: #D1D5DB;
    --color-border-focus: #3B82F6;
    
    /* ─── Typography Tokens ─── */
    --font-family-sans: 'Inter', system-ui, -apple-system, sans-serif;
    --font-size-xs: 0.75rem;
    --font-size-sm: 0.875rem;
    --font-size-base: 1rem;
    --font-size-lg: 1.125rem;
    --font-size-xl: 1.25rem;
    --font-size-2xl: 1.5rem;
    --font-size-3xl: 1.875rem;
    --font-size-4xl: 2.25rem;
    --font-weight-normal: 400;
    --font-weight-medium: 500;
    --font-weight-semibold: 600;
    --font-weight-bold: 700;
    --line-height-tight: 1.25;
    --line-height-normal: 1.5;
    --line-height-relaxed: 1.625;
    
    /* ─── Spacing Tokens (4px base) ─── */
    --spacing-0: 0px;
    --spacing-1: 0.25rem;   /* 4px */
    --spacing-2: 0.5rem;    /* 8px */
    --spacing-3: 0.75rem;   /* 12px */
    --spacing-4: 1rem;      /* 16px */
    --spacing-5: 1.25rem;   /* 20px */
    --spacing-6: 1.5rem;    /* 24px */
    --spacing-8: 2rem;      /* 32px */
    --spacing-10: 2.5rem;   /* 40px */
    --spacing-12: 3rem;     /* 48px */
    --spacing-16: 4rem;     /* 64px */
    --spacing-20: 5rem;     /* 80px */
    
    /* ─── Border Radius Tokens ─── */
    --radius-none: 0px;
    --radius-sm: 0.25rem;
    --radius-md: 0.375rem;
    --radius-lg: 0.5rem;
    --radius-xl: 1rem;
    --radius-2xl: 1.5rem;
    --radius-full: 9999px;
    
    /* ─── Shadow / Elevation Tokens ─── */
    --shadow-sm: 0 1px 2px 0 rgba(0, 0, 0, 0.05);
    --shadow-md: 0 4px 6px -1px rgba(0, 0, 0, 0.1), 0 2px 4px -2px rgba(0, 0, 0, 0.1);
    --shadow-lg: 0 10px 15px -3px rgba(0, 0, 0, 0.1), 0 4px 6px -4px rgba(0, 0, 0, 0.1);
    --shadow-xl: 0 20px 25px -5px rgba(0, 0, 0, 0.1), 0 8px 10px -6px rgba(0, 0, 0, 0.1);
    --shadow-soft: 0 2px 15px -3px rgba(0, 0, 0, 0.07), 0 10px 20px -2px rgba(0, 0, 0, 0.04);
    
    /* ─── Transition / Animation Tokens ─── */
    --transition-fast: 150ms;
    --transition-normal: 200ms;
    --transition-slow: 300ms;
    --ease-out: cubic-bezier(0.16, 1, 0.3, 1);
    --ease-in-out: cubic-bezier(0.4, 0, 0.2, 1);
    
    /* ─── Z-Index Scale Tokens ─── */
    --z-dropdown: 50;
    --z-sticky: 100;
    --z-fixed: 200;
    --z-modal-backdrop: 300;
    --z-modal: 400;
    --z-toast: 500;
  }
}

@layer components {
  /* Keep existing component classes */
  .btn-primary {
    @apply px-6 py-2.5 bg-primary text-white font-medium rounded-xl hover:bg-blue-600 focus:ring-2 focus:ring-primary focus:ring-offset-2 transition-all duration-200;
  }
  .btn-secondary {
    @apply px-6 py-2.5 bg-white text-gray-700 font-medium rounded-xl border border-gray-200 hover:bg-gray-50 focus:ring-2 focus:ring-primary focus:ring-offset-2 transition-all duration-200;
  }
  .card {
    @apply bg-white rounded-2xl shadow-soft border border-gray-100;
  }
  .input-field {
    @apply w-full px-4 py-3 border border-gray-300 rounded-xl focus:ring-2 focus:ring-primary focus:border-transparent transition-all duration-200;
  }
}
```

Implementation notes:
- Preserve the existing `@import url('...')` for Inter font at the top
- Add the complete `:root` block with ALL tokens listed above
- Keep the component layer classes (btn-primary, btn-secondary, card, input-field)
- The file should have `@tailwind` directives first, then the `@layer base` with `:root`, then `@layer components`
</action>
  <verify>
    <automated>grep -q ":root {" wwwroot/css/tailwind.css && grep -q "--color-primary-500" wwwroot/css/tailwind.css && grep -q "--font-family-sans" wwwroot/css/tailwind.css && grep -q "--spacing-4" wwwroot/css/tailwind.css && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - wwwroot/css/tailwind.css contains ":root {" with CSS custom properties
    - wwwroot/css/tailwind.css contains "--color-primary-500: #3B82F6"
    - wwwroot/css/tailwind.css contains "--color-secondary-500: #10B981"
    - wwwroot/css/tailwind.css contains "--color-error-500: #EF4444"
    - wwwroot/css/tailwind.css contains "--font-family-sans" with Inter
    - wwwroot/css/tailwind.css contains "--spacing-4: 1rem"
    - wwwroot/css/tailwind.css contains "--shadow-soft" token
    - wwwroot/css/tailwind.css contains "--transition-normal: 200ms"
    - wwwroot/css/tailwind.css contains "--z-toast: 500"
    - wwwroot/css/tailwind.css still has @tailwind directives
  </acceptance_criteria>
  <done>CSS custom property tokens defined for color, typography, spacing, shadows, transitions, and z-index</done>
</task>

<task type="auto">
  <name>Task 2: Extend Tailwind Config to Reference Token Values</name>
  <files>tailwind.config.js</files>
  <read_first>
    - tailwind.config.js (current state — extend with more tokens)
    - wwwroot/css/tailwind.css (reference token values just created)
  </read_first>
  <action>
Replace `tailwind.config.js` with extended configuration that adds:
- More color variants (match the CSS custom property palette)
- Full typography scale
- Spacing scale
- Extended shadows
- Animation utilities
- Container and z-index config

```javascript
/** @type {import('tailwindcss').Config} */
module.exports = {
  content: [
    "./Views/**/*.cshtml",
    "./wwwroot/js/**/*.js",
  ],
  theme: {
    extend: {
      colors: {
        primary: {
          50: "#EFF6FF",
          100: "#DBEAFE",
          200: "#BFDBFE",
          300: "#93C5FD",
          400: "#60A5FA",
          500: "#3B82F6",
          600: "#2563EB",
          700: "#1D4ED8",
          DEFAULT: "#3B82F6",
        },
        secondary: {
          50: "#ECFDF5",
          100: "#D1FAE5",
          200: "#A7F3D0",
          400: "#34D399",
          500: "#10B981",
          600: "#059669",
          700: "#047857",
          DEFAULT: "#10B981",
        },
        error: {
          50: "#FEF2F2",
          100: "#FEE2E2",
          400: "#F87171",
          500: "#EF4444",
          600: "#DC2626",
          DEFAULT: "#EF4444",
        },
        warning: {
          50: "#FFFBEB",
          100: "#FEF3C7",
          400: "#FBBF24",
          500: "#F59E0B",
          600: "#D97706",
          DEFAULT: "#F59E0B",
        },
        // Keep short aliases for backward compatibility
        primary: "#3B82F6",
        secondary: "#10B981",
        error: "#EF4444",
        warning: "#F59E0B",
      },
      fontFamily: {
        sans: ["Inter", "system-ui", "sans-serif"],
      },
      fontSize: {
        xs: ["0.75rem", { lineHeight: "1rem" }],
        sm: ["0.875rem", { lineHeight: "1.25rem" }],
        base: ["1rem", { lineHeight: "1.5rem" }],
        lg: ["1.125rem", { lineHeight: "1.75rem" }],
        xl: ["1.25rem", { lineHeight: "1.75rem" }],
        "2xl": ["1.5rem", { lineHeight: "2rem" }],
        "3xl": ["1.875rem", { lineHeight: "2.25rem" }],
        "4xl": ["2.25rem", { lineHeight: "2.5rem" }],
      },
      borderRadius: {
        xl: "1rem",
        "2xl": "1.5rem",
      },
      boxShadow: {
        soft: "0 2px 15px -3px rgba(0, 0, 0, 0.07), 0 10px 20px -2px rgba(0, 0, 0, 0.04)",
      },
      transitionDuration: {
        fast: "150ms",
        normal: "200ms",
        slow: "300ms",
      },
      zIndex: {
        dropdown: "50",
        sticky: "100",
        fixed: "200",
        modal: "400",
        toast: "500",
      },
    },
  },
  plugins: [],
};
```

Note: Keep the primary/secondary/error/warning shorthand aliases (`primary: "#3B82F6"`) for backward compatibility with existing views that use `bg-primary`, `text-primary`, etc. The expanded color scale (primary-50 through primary-700) is additive for more precise control.
</action>
  <verify>
    <automated>grep -q "primary.*50" tailwind.config.js && grep -q "transitionDuration" tailwind.config.js && grep -q "zIndex.*toast" tailwind.config.js && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - tailwind.config.js contains "primary" color with 50-700 variants
    - tailwind.config.js contains "secondary" color with 50-700 variants
    - tailwind.config.js contains "error" color with 50-600 variants
    - tailwind.config.js contains "warning" color with 50-600 variants
    - tailwind.config.js contains "transitionDuration" with fast/normal/slow
    - tailwind.config.js contains "zIndex" with toast: "500"
    - tailwind.config.js still has theme.extend
    - backward-compatible primary/secondary/error/warning shorthand aliases preserved
  </acceptance_criteria>
  <done>Tailwind config extended with full color scales, transition tokens, and z-index tokens</done>
</task>

<task type="auto">
  <name>Task 3: Create Design Token Documentation</name>
  <files>.planning/design-tokens.md</files>
  <read_first>
    - wwwroot/css/tailwind.css (source of truth for token values)
    - tailwind.config.js (Tailwind-specific mappings)
  </read_first>
  <action>
Create `.planning/design-tokens.md` as the canonical design token reference. This file documents every token defined in tailwind.css with its purpose, usage, and visual examples.

Structure:
```markdown
# Design Tokens — ItransitionTemplates

**Last updated:** 2026-07-14
**Source of truth:** `wwwroot/css/tailwind.css` (`:root` block)
**Consumed by:** All CSHTML views via Tailwind utility classes and CSS custom properties

## Quick Reference

| Token Category | Files | Prefix |
|----------------|-------|--------|
| Color | tailwind.css, tailwind.config.js | `--color-` |
| Typography | tailwind.css, tailwind.config.js | `--font-`, `--font-size-` |
| Spacing | tailwind.css | `--spacing-` |
| Border Radius | tailwind.css, tailwind.config.js | `--radius-` |
| Shadows | tailwind.css, tailwind.config.js | `--shadow-` |
| Transitions | tailwind.css, tailwind.config.js | `--transition-` |
| Z-Index | tailwind.css, tailwind.config.js | `--z-` |

---

## Color Tokens

### Primary (Blue) — `#3B82F6`
Used for: primary actions, links, active states, brand elements

| Token | Value | Usage |
|-------|-------|-------|
| `--color-primary-50` | #EFF6FF | Hover backgrounds |
| `--color-primary-100` | #DBEAFE | Light backgrounds |
| `--color-primary-500` | #3B82F6 | **Default primary** |
| `--color-primary-600` | #2563EB | Hover state |
| `--color-primary-700` | #1D4ED8 | Active state |

Tailwind class: `bg-primary`, `text-primary`, `hover:bg-primary/10`, `focus:ring-primary`

### Secondary (Emerald) — `#10B981`
Used for: success states, confirmations, positive actions

| Token | Value | Usage |
|-------|-------|-------|
| `--color-secondary-500` | #10B981 | **Default secondary** |
| `--color-secondary-600` | #059669 | Hover state |

Tailwind class: `bg-secondary`, `text-secondary`, `hover:bg-secondary/10`

### Error (Red) — `#EF4444`
Used for: destructive actions, error messages, validation errors

| Token | Value | Usage |
|-------|-------|-------|
| `--color-error-50` | #FEF2F2 | Error background |
| `--color-error-500` | #EF4444 | **Default error** |
| `--color-error-600` | #DC2626 | Hover state |

Tailwind class: `bg-error`, `text-error`, `border-error`, `hover:bg-error/10`

### Warning (Amber) — `#F59E0B`
Used for: warnings, cautions, pending states

| Token | Value | Usage |
|-------|-------|-------|
| `--color-warning-500` | #F59E0B | **Default warning** |
| Tailwind class: `bg-warning`, `text-warning` |

### Gray / Neutral Scale
Used for: backgrounds, borders, text, dividers

| Token | Value | Usage |
|-------|-------|-------|
| `--color-gray-50` | #F9FAFB | Page background |
| `--color-gray-100` | #F3F4F6 | Card/section background |
| `--color-gray-200` | #E5E7EB | Borders, dividers |
| `--color-gray-300` | #D1D5DB | Disabled borders |
| `--color-gray-400` | #9CA3AF | Placeholder text |
| `--color-gray-500` | #6B7280 | Secondary text |
| `--color-gray-600` | #4B5563 | Body text |
| `--color-gray-700` | #374151 | Heading text |
| `--color-gray-800` | #1F2937 | Strong headings |
| `--color-gray-900` | #111827 | Primary text |

### Semantic Color Aliases

| Token | Value | Maps To | Purpose |
|-------|-------|---------|---------|
| `--color-bg-primary` | #FFFFFF | — | Main page background |
| `--color-bg-secondary` | #F9FAFB | gray-50 | Secondary background |
| `--color-text-primary` | #111827 | gray-900 | Primary text color |
| `--color-text-secondary` | #6B7280 | gray-500 | Secondary/muted text |
| `--color-text-inverse` | #FFFFFF | — | Text on dark backgrounds |
| `--color-border-default` | #E5E7EB | gray-200 | Default border color |
| `--color-border-focus` | #3B82F6 | primary-500 | Focus ring color |

---

## Typography Tokens

### Font Family
- Primary: `Inter` (loaded via Google Fonts)
- Fallback: `system-ui, -apple-system, sans-serif`
- Tailwind class: `font-sans` (configured in tailwind.config.js)

### Font Size Scale

| Token | Value | Tailwind | Used For |
|-------|-------|----------|----------|
| `--font-size-xs` | 0.75rem (12px) | `text-xs` | Labels, captions |
| `--font-size-sm` | 0.875rem (14px) | `text-sm` | Secondary text |
| `--font-size-base` | 1rem (16px) | `text-base` | Body text |
| `--font-size-lg` | 1.125rem (18px) | `text-lg` | Large body |
| `--font-size-xl` | 1.25rem (20px) | `text-xl` | Subheadings |
| `--font-size-2xl` | 1.5rem (24px) | `text-2xl` | Section headings |
| `--font-size-3xl` | 1.875rem (30px) | `text-3xl` | Page headings |
| `--font-size-4xl` | 2.25rem (36px) | `text-4xl` | Hero headings |

### Font Weights
| Token | Value | Tailwind | Used For |
|-------|-------|----------|----------|
| `--font-weight-normal` | 400 | `font-normal` | Body text |
| `--font-weight-medium` | 500 | `font-medium` | Emphasized text |
| `--font-weight-semibold` | 600 | `font-semibold` | Subheadings |
| `--font-weight-bold` | 700 | `font-bold` | Headings |

---

## Spacing Tokens (4px Base)

| Token | Value | Tailwind | Used For |
|-------|-------|----------|----------|
| `--spacing-0` | 0px | `p-0`, `m-0` | No spacing |
| `--spacing-1` | 0.25rem (4px) | `p-1`, `gap-1` | Micro spacing |
| `--spacing-2` | 0.5rem (8px) | `p-2`, `gap-2` | Tight spacing |
| `--spacing-3` | 0.75rem (12px) | `p-3`, `gap-3` | Compact spacing |
| `--spacing-4` | 1rem (16px) | `p-4`, `gap-4` | **Default spacing** |
| `--spacing-6` | 1.5rem (24px) | `p-6`, `gap-6` | Section padding |
| `--spacing-8` | 2rem (32px) | `p-8`, `gap-8` | Card padding |
| `--spacing-12` | 3rem (48px) | `p-12`, `gap-12` | Large sections |
| `--spacing-16` | 4rem (64px) | `p-16`, `gap-16` | Page sections |
| `--spacing-20` | 5rem (80px) | `p-20` | Hero sections |

---

## Border Radius Tokens

| Token | Value | Tailwind | Used For |
|-------|-------|----------|----------|
| `--radius-sm` | 0.25rem | `rounded-sm` | Inputs, badges |
| `--radius-md` | 0.375rem | `rounded-md` | Buttons |
| `--radius-lg` | 0.5rem | `rounded-lg` | Cards, containers |
| `--radius-xl` | 1rem | `rounded-xl` | **Default card radius** |
| `--radius-2xl` | 1.5rem | `rounded-2xl` | Large cards, modals |
| `--radius-full` | 9999px | `rounded-full` | Avatars, pills |

---

## Shadow Tokens

| Token | Value | Tailwind | Used For |
|-------|-------|----------|----------|
| `--shadow-sm` | 0 1px 2px rgba(0,0,0,0.05) | `shadow-sm` | Subtle elevation |
| `--shadow-md` | 0 4px 6px -1px rgba(0,0,0,0.1) | `shadow-md` | Cards |
| `--shadow-lg` | 0 10px 15px -3px rgba(0,0,0,0.1) | `shadow-lg` | Dropdowns, modals |
| `--shadow-xl` | 0 20px 25px -5px rgba(0,0,0,0.1) | `shadow-xl` | Large modals |
| `--shadow-soft` | 0 2px 15px -3px rgba(0,0,0,0.07) | `shadow-soft` | **Default card shadow** |

---

## Transition Tokens

| Token | Value | Tailwind | Used For |
|-------|-------|----------|----------|
| `--transition-fast` | 150ms | `duration-fast` | Micro-interactions |
| `--transition-normal` | 200ms | `duration-normal` | Hover states |
| `--transition-slow` | 300ms | `duration-slow` | Panel slide, modals |
| `--ease-out` | cubic-bezier(0.16,1,0.3,1) | `ease-out` | Elements entering |
| `--ease-in-out` | cubic-bezier(0.4,0,0.2,1) | `ease-in-out` | Toggle transitions |

---

## Z-Index Scale Tokens

| Token | Value | Purpose |
|-------|-------|---------|
| `--z-dropdown` | 50 | Dropdown menus |
| `--z-sticky` | 100 | Sticky headers |
| `--z-fixed` | 200 | Fixed navbars |
| `--z-modal-backdrop` | 300 | Modal backdrops |
| `--z-modal` | 400 | Modal dialogs |
| `--z-toast` | 500 | Toast notifications |

---

## Usage Examples

### In .cshtml views (preferred — use Tailwind utility classes):
```html
<button class="bg-primary text-white px-6 py-2.5 rounded-xl hover:bg-primary-600 transition-colors duration-fast">
  Submit
</button>
<div class="card p-8">
  <h2 class="text-2xl font-bold text-gray-900">Title</h2>
  <p class="text-gray-500 mt-2">Description</p>
</div>
```

### In JavaScript (for dynamically created elements):
```javascript
element.className = 'px-4 py-3 border border-gray-300 rounded-xl focus:ring-2 focus:ring-primary';
```

### Via CSS custom properties (for component styles in tailwind.css):
```css
.custom-element {
  background-color: var(--color-bg-secondary);
  border: 1px solid var(--color-border-default);
  border-radius: var(--radius-xl);
  box-shadow: var(--shadow-soft);
  transition: box-shadow var(--transition-normal) var(--ease-out);
}
```

---

## Adding New Tokens

1. Add the token value to `:root` in `wwwroot/css/tailwind.css`
2. Add a Tailwind utility mapping in `tailwind.config.js` (if needed as a utility class)
3. Document the new token in this file
4. Update existing views to use the new token where appropriate
```
  </action>
  <verify>
    <automated>test -f .planning/design-tokens.md && grep -q "## Color Tokens" .planning/design-tokens.md && grep -q "## Spacing Tokens" .planning/design-tokens.md && grep -q "## Typography Tokens" .planning/design-tokens.md && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - .planning/design-tokens.md exists
    - Contains "## Color Tokens" section
    - Contains "## Typography Tokens" section
    - Contains "## Spacing Tokens" section
    - Contains "## Border Radius Tokens" section
    - Contains "## Shadow Tokens" section
    - Contains "## Transition Tokens" section
    - Contains "## Z-Index Scale Tokens" section
    - Contains "## Usage Examples" section
  </acceptance_criteria>
  <done>Design token documentation created as single source of truth</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CSS → Browser | CSS custom properties applied client-side, no injection risk |
| Config → Build | tailwind.config.js is local-only, no external input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Tampering | CSS custom properties | accept | CSS properties are static, no dynamic injection |
| T-02-02 | Information Disclosure | Token documentation | accept | Design tokens are public UI values, no sensitive data |
</threat_model>

<verification>
1. Read wwwroot/css/tailwind.css — verify :root block contains all token categories
2. Read tailwind.config.js — verify extended color scales and token mappings
3. Read .planning/design-tokens.md — verify all token sections documented
4. Run `dotnet build` — ensure no compilation errors (token changes are CSS-only)
5. Spot-check a view uses a token value correctly (e.g., `bg-primary` still renders blue)
</verification>

<success_criteria>
- All CSS custom property tokens defined in tailwind.css :root block
- Tailwind config extended with color scales, transition durations, and z-index tokens
- Design token documentation created with all token categories
- Backward compatibility maintained (existing `bg-primary`, `text-secondary` etc. still work)
- `dotnet build` passes with 0 errors
- No visual regressions on existing views
</success_criteria>

<output>
Create `.planning/phases/02-design-tokens/02-01-SUMMARY.md` when done
</output>
