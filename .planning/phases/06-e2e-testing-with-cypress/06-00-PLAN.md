---
phase: 06-e2e-testing-with-cypress
plan: 00
type: execute
wave: 0
depends_on: []
files_modified:
  - Views/Shared/_Layout.cshtml
  - Views/User/LogInView.cshtml
  - Views/User/SignUpView.cshtml
  - Views/Home/Index.cshtml
  - Views/Template/CreateTemplate.cshtml
  - Views/Template/TemplateView.cshtml
  - wwwroot/js/utils/templates/printTemplates.js
  - wwwroot/js/createTemplate/BaseQuestion.js
  - wwwroot/js/createTemplate/Multiple-options-question.js
autonomous: true
requirements: []

must_haves:
  truths:
    - "All interactive elements in Razor views have data-cy attributes"
    - "All dynamically created elements in JS files have data-cy attributes"
    - "data-cy attributes use kebab-case naming convention"
    - "View-prefixed names used where IDs overlap (login-email, signup-email)"
    - "No existing functionality is broken by adding data-cy attributes"
  artifacts:
    - path: "Views/Shared/_Layout.cshtml"
      provides: "Layout with data-cy attributes on navbar, sidebar, auth links"
      contains: "data-cy"
    - path: "Views/User/LogInView.cshtml"
      provides: "Login form with data-cy attributes"
      contains: "data-cy"
    - path: "Views/User/SignUpView.cshtml"
      provides: "Signup form with data-cy attributes"
      contains: "data-cy"
    - path: "Views/Home/Index.cshtml"
      provides: "Home page with data-cy attributes on search, templates, actions"
      contains: "data-cy"
    - path: "Views/Template/CreateTemplate.cshtml"
      provides: "Template builder with data-cy attributes on all controls and modals"
      contains: "data-cy"
    - path: "Views/Template/TemplateView.cshtml"
      provides: "Template view with data-cy attributes on like, response form"
      contains: "data-cy"
    - path: "wwwroot/js/utils/templates/printTemplates.js"
      provides: "Template cards with data-cy attributes on dynamic links"
      contains: "data-cy"
    - path: "wwwroot/js/createTemplate/BaseQuestion.js"
      provides: "Question elements with data-cy attributes on delete buttons"
      contains: "data-cy"
    - path: "wwwroot/js/createTemplate/Multiple-options-question.js"
      provides: "Multi-choice elements with data-cy attributes on edit/delete buttons"
      contains: "data-cy"
  key_links:
    - from: "Views/User/LogInView.cshtml"
      to: "cypress/support/commands.js"
      via: "data-cy selectors for login form"
      pattern: "data-cy=\"login-"
    - from: "Views/User/SignUpView.cshtml"
      to: "cypress/support/commands.js"
      via: "data-cy selectors for signup form"
      pattern: "data-cy=\"signup-"
---

<objective>
Add `data-cy` test attributes to all interactive elements across Razor views and JavaScript files.

Purpose: Provide stable, dedicated selectors for Cypress E2E tests. Using `data-cy` attributes instead of CSS classes/IDs/tags ensures tests don't break when styling or markup changes.

Output: All interactive elements tagged with `data-cy` attributes following kebab-case naming convention with view-prefixed names where IDs overlap.
</objective>

<execution_context>
@/home/victor/.config/opencode/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/06-e2e-testing-with-cypress/06-CONTEXT.md
@Views/Shared/_Layout.cshtml
@Views/User/LogInView.cshtml
@Views/User/SignUpView.cshtml
@Views/Home/Index.cshtml
@Views/Template/CreateTemplate.cshtml
@Views/Template/TemplateView.cshtml
@wwwroot/js/utils/templates/printTemplates.js
@wwwroot/js/createTemplate/BaseQuestion.js
@wwwroot/js/createTemplate/Multiple-options-question.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add data-cy to _Layout.cshtml</name>
  <files>Views/Shared/_Layout.cshtml</files>
  <read_first>
    - Views/Shared/_Layout.cshtml (identify all interactive elements)
  </read_first>
  <action>
Add `data-cy` attributes to these elements in _Layout.cshtml:

1. Mobile hamburger button (line ~47): `data-cy="mobile-hamburger"`
2. Logo/home link (line ~55): `data-cy="logo-home-link"`
3. Navbar search input (line ~71): `data-cy="navbar-search"`
4. User avatar button (line ~88): `data-cy="user-avatar-btn"`
5. Sign out link (line ~105): `data-cy="sign-out-link"`
6. Sign in link (line ~113): `data-cy="sign-in-link"`
7. Get started link (line ~116): `data-cy="get-started-link"`
8. Mobile sidebar overlay (line ~125): `data-cy="sidebar-overlay"`
9. Mobile sidebar home link (line ~149): `data-cy="sidebar-home-link"`
10. Mobile sidebar create template link (line ~158): `data-cy="sidebar-create-template-link"`
11. Mobile sidebar log in link (line ~168): `data-cy="sidebar-login-link"`
12. Desktop sidebar home link (line ~184): `data-cy="desktop-sidebar-home-link"`
13. Desktop sidebar create template link (line ~193): `data-cy="desktop-sidebar-create-template-link"`
14. Desktop sidebar log in link (line ~203): `data-cy="desktop-sidebar-login-link"`
15. Toast container (line ~228): `data-cy="toast-container"`
  </action>
  <verify>
    <automated>grep -c 'data-cy=' Views/Shared/_Layout.cshtml | xargs test 15 -eq && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - All 15 interactive elements have data-cy attributes
    - Attributes use kebab-case naming
    - No existing attributes are removed or modified
    - HTML structure remains valid
  </acceptance_criteria>
  <done>Layout view tagged with data-cy attributes for all navbar and sidebar elements</done>
</task>

<task type="auto">
  <name>Task 2: Add data-cy to LogInView.cshtml</name>
  <files>Views/User/LogInView.cshtml</files>
  <read_first>
    - Views/User/LogInView.cshtml (identify all form elements)
  </read_first>
  <action>
Add `data-cy` attributes to these elements in LogInView.cshtml:

1. Error container (line ~23): `data-cy="login-error-container"`
2. Login form (line ~26): `data-cy="login-form"`
3. Email input (line ~30): `data-cy="login-email"`
4. Email error message (line ~38): `data-cy="login-email-error"`
5. Password input (line ~45): `data-cy="login-password"`
6. Toggle password button (line ~53): `data-cy="login-toggle-password"`
7. Password error message (line ~68): `data-cy="login-password-error"`
8. Submit button (line ~72): `data-cy="login-submit-btn"`
9. Sign up footer link (line ~88): `data-cy="login-signup-link"`
  </action>
  <verify>
    <automated>grep -c 'data-cy=' Views/User/LogInView.cshtml | xargs test 9 -eq && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - All 9 form elements have data-cy attributes with "login-" prefix
    - Attributes use kebab-case naming
    - No existing attributes are removed or modified
  </acceptance_criteria>
  <done>Login view tagged with data-cy attributes for all form elements</done>
</task>

<task type="auto">
  <name>Task 3: Add data-cy to SignUpView.cshtml</name>
  <files>Views/User/SignUpView.cshtml</files>
  <read_first>
    - Views/User/SignUpView.cshtml (identify all form elements)
  </read_first>
  <action>
Add `data-cy` attributes to these elements in SignUpView.cshtml:

1. Error container (line ~23): `data-cy="signup-error-container"`
2. Sign up form (line ~26): `data-cy="signup-form"`
3. Email input (line ~30): `data-cy="signup-email"`
4. Email error message (line ~38): `data-cy="signup-email-error"`
5. Username input (line ~44): `data-cy="signup-username"`
6. Username error message (line ~52): `data-cy="signup-username-error"`
7. Password input (line ~58): `data-cy="signup-password"`
8. Password error message (line ~66): `data-cy="signup-password-error"`
9. Strength bar 1 (line ~70): `data-cy="signup-strength-bar-1"`
10. Strength bar 2 (line ~71): `data-cy="signup-strength-bar-2"`
11. Strength bar 3 (line ~72): `data-cy="signup-strength-bar-3"`
12. Strength bar 4 (line ~73): `data-cy="signup-strength-bar-4"`
13. Strength text (line ~75): `data-cy="signup-strength-text"`
14. Confirm password input (line ~83): `data-cy="signup-confirm-password"`
15. Match icon (line ~89): `data-cy="signup-match-icon"`
16. Confirm error message (line ~91): `data-cy="signup-confirm-error"`
17. Password requirement length (line ~98): `data-cy="signup-req-length"`
18. Submit button (line ~105): `data-cy="signup-submit-btn"`
19. Sign in footer link (line ~122): `data-cy="signup-signin-link"`
  </action>
  <verify>
    <automated>grep -c 'data-cy=' Views/User/SignUpView.cshtml | xargs test 19 -eq && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - All 19 form elements have data-cy attributes with "signup-" prefix
    - Attributes use kebab-case naming
    - No existing attributes are removed or modified
  </acceptance_criteria>
  <done>Signup view tagged with data-cy attributes for all form elements</done>
</task>

<task type="auto">
  <name>Task 4: Add data-cy to Home/Index.cshtml</name>
  <files>Views/Home/Index.cshtml</files>
  <read_first>
    - Views/Home/Index.cshtml (identify all interactive elements)
  </read_first>
  <action>
Add `data-cy` attributes to these elements in Home/Index.cshtml:

1. Create template link (authenticated hero, line ~23): `data-cy="home-create-template-link"`
2. Hidden userId input (line ~31): `data-cy="home-user-id"`
3. Hidden username input (line ~32): `data-cy="home-username"`
4. Get started hero link (guest, line ~40): `data-cy="home-hero-signup-link"`
5. Search input (line ~57): `data-cy="home-search-input"`
6. Search results container (line ~64): `data-cy="home-search-results"`
7. Templates header (line ~70): `data-cy="home-templates-header"`
8. Toggle templates button (line ~73): `data-cy="home-toggle-templates-btn"`
9. Templates grid container (line ~81): `data-cy="home-templates-container"`
10. Empty state container (line ~101): `data-cy="home-empty-state"`
11. Empty state create link (line ~107): `data-cy="home-empty-create-link"`
12. Load more button (line ~117): `data-cy="home-load-more-btn"`
  </action>
  <verify>
    <automated>grep -c 'data-cy=' Views/Home/Index.cshtml | xargs test 12 -eq && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - All 12 elements have data-cy attributes with "home-" prefix
    - Attributes use kebab-case naming
    - No existing attributes are removed or modified
  </acceptance_criteria>
  <done>Home page tagged with data-cy attributes for all interactive elements</done>
</task>

<task type="auto">
  <name>Task 5: Add data-cy to CreateTemplate.cshtml</name>
  <files>Views/Template/CreateTemplate.cshtml</files>
  <read_first>
    - Views/Template/CreateTemplate.cshtml (identify all controls, modals, form elements)
  </read_first>
  <action>
Add `data-cy` attributes to these elements in CreateTemplate.cshtml:

**Main area:**
1. Server responses (line ~8): `data-cy="server-responses"`
2. Page title (line ~12): `data-cy="page-title"`
3. Topic select (line ~19): `data-cy="template-topic-select"`
4. Create template button (line ~33): `data-cy="btn-create-template"`
5. Add single line button (line ~43): `data-cy="add-single-line-btn"`
6. Add paragraph button (line ~51): `data-cy="add-paragraph-btn"`
7. Add number button (line ~59): `data-cy="add-number-btn"`
8. Add checkboxes button (line ~67): `data-cy="add-checkboxes-btn"`
9. Add multiple choice button (line ~75): `data-cy="add-multiple-choice-btn"`
10. Template title (line ~87): `data-cy="template-title-input"`
11. Template description (line ~90): `data-cy="template-description-input"`
12. Visibility toggle (line ~95): `data-cy="visibility-toggle"`
13. Visibility select (line ~104): `data-cy="visibility-select"`
14. Add admin button (line ~112): `data-cy="btn-add-admin"`
15. Add allowed users button (line ~116): `data-cy="btn-add-allowed-users"`
16. Template questions form (line ~125): `data-cy="template-questions-form"`
17. Email input (line ~128): `data-cy="template-email-input"`
18. Date input (line ~132): `data-cy="template-date-input"`
19. Danger zone container (line ~138): `data-cy="danger-zone"`
20. Delete template trigger button (line ~141): `data-cy="btn-delete-template-trigger"`

**Admin modal:**
21. Modal title (line ~168): `data-cy="admin-modal-title"`
22. Close admin modal X button (line ~169): `data-cy="admin-modal-close-btn"`
23. Search user input (line ~179): `data-cy="admin-search-user-input"`
24. Search results (line ~181): `data-cy="admin-search-results"`
25. Users container (line ~187): `data-cy="admin-users-container"`
26. Cancel admin modal button (line ~192): `data-cy="admin-modal-cancel-btn"`
27. Save users button (line ~194): `data-cy="admin-modal-save-btn"`

**Options modal:**
28. Close options modal X button (line ~220): `data-cy="options-modal-close-btn"`
29. Options textarea (line ~232): `data-cy="options-textarea"`
30. Options list (line ~237): `data-cy="options-list"`
31. Cancel options modal button (line ~243): `data-cy="options-modal-cancel-btn"`
32. Save options changes button (line ~245): `data-cy="options-modal-save-btn"`

**Delete modal:**
33. Close delete modal X button (line ~271): `data-cy="delete-modal-close-btn"`
34. Delete confirm input (line ~283): `data-cy="delete-confirm-input"`
35. Cancel delete modal button (line ~289): `data-cy="delete-modal-cancel-btn"`
36. Confirm delete button (line ~291): `data-cy="delete-confirm-btn"`
  </action>
  <verify>
    <automated>grep -c 'data-cy=' Views/Template/CreateTemplate.cshtml | xargs test 36 -eq && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - All 36 elements have data-cy attributes
    - Attributes use kebab-case naming
    - Modal elements use contextual prefixes (admin-modal-*, options-modal-*, delete-modal-*)
    - No existing attributes are removed or modified
  </acceptance_criteria>
  <done>Template builder tagged with data-cy attributes for all controls and modals</done>
</task>

<task type="auto">
  <name>Task 6: Add data-cy to TemplateView.cshtml</name>
  <files>Views/Template/TemplateView.cshtml</files>
  <read_first>
    - Views/Template/TemplateView.cshtml (identify all interactive elements)
  </read_first>
  <action>
Add `data-cy` attributes to these elements in TemplateView.cshtml:

1. Server messages (line ~10): `data-cy="server-msgs"`
2. Error go home link (line ~23): `data-cy="error-go-home-link"`
3. Form header (line ~34): `data-cy="form-header"`
4. Form title (line ~37): `data-cy="form-title"`
5. Form description (line ~38): `data-cy="form-description"`
6. Likes container (line ~40): `data-cy="likes-container"`
7. Log out button container (line ~43): `data-cy="log-out-container"`
8. Likes number (line ~44): `data-cy="likes-number"`
9. Like template button (line ~45): `data-cy="btn-like-template"`
10. Progress bar (line ~63): `data-cy="progress-bar"`
11. Progress text (line ~65): `data-cy="progress-text"`
12. Response form (line ~67): `data-cy="response-form"`
13. Response email input (line ~71): `data-cy="response-email"`
14. Response date input (line ~75): `data-cy="response-date"`
15. Submit response button (line ~82): `data-cy="btn-submit-response"`
16. Success state container (line ~90): `data-cy="success-state"`
  </action>
  <verify>
    <automated>grep -c 'data-cy=' Views/Template/TemplateView.cshtml | xargs test 16 -eq && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - All 16 elements have data-cy attributes
    - Attributes use kebab-case naming
    - No existing attributes are removed or modified
  </acceptance_criteria>
  <done>Template view tagged with data-cy attributes for all interactive elements</done>
</task>

<task type="auto">
  <name>Task 7: Add data-cy to dynamic JS elements</name>
  <files>wwwroot/js/utils/templates/printTemplates.js, wwwroot/js/createTemplate/BaseQuestion.js, wwwroot/js/createTemplate/Multiple-options-question.js</files>
  <read_first>
    - wwwroot/js/utils/templates/printTemplates.js (template card HTML generation)
    - wwwroot/js/createTemplate/BaseQuestion.js (question container and delete button creation)
    - wwwroot/js/createTemplate/Multiple-options-question.js (edit options and delete button creation)
  </read_first>
  <action>
1. In printTemplates.js:
   - In `createTemplateCard()`, add `data-cy="template-card-link"` to the `<a>` element (line ~55)

2. In BaseQuestion.js:
   - In `addEditionControls()`, add `data-cy="delete-question-btn"` to the delete button (line ~43)

3. In Multiple-options-question.js:
   - In `addEditionControls()`, add `data-cy="edit-options-btn"` to the edit options button (line ~39)
   - In `addEditionControls()`, add `data-cy="delete-question-btn"` to the delete button (line ~38)
   - In the click handler for edit options, add `data-cy="delete-option-btn"` to the delete option button (line ~60)
  </action>
  <verify>
    <automated>grep -c 'data-cy' wwwroot/js/utils/templates/printTemplates.js wwwroot/js/createTemplate/BaseQuestion.js wwwroot/js/createTemplate/Multiple-options-question.js | grep -v ':0$' | wc -l | xargs test 3 -eq && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - Template card links have data-cy="template-card-link"
    - Question delete buttons have data-cy="delete-question-btn"
    - Edit options buttons have data-cy="edit-options-btn"
    - Option delete buttons have data-cy="delete-option-btn"
    - No existing functionality is broken
  </acceptance_criteria>
  <done>Dynamic JS elements tagged with data-cy attributes for Cypress selection</done>
</task>

</tasks>

<verification>
1. Run `grep -r 'data-cy=' Views/ | wc -l` to verify total count across all views
2. Run `grep -r 'data-cy' wwwroot/js/ | wc -l` to verify JS files
3. Verify no duplicate data-cy values within the same view
4. Verify all data-cy values use kebab-case
5. Open the application in browser and inspect elements to confirm attributes render correctly
</verification>

<success_criteria>
- All interactive elements across 6 Razor views have data-cy attributes
- Dynamic elements in 3 JS files have data-cy attributes
- Naming convention: kebab-case with view prefix where IDs overlap
- Total: ~108 static elements + ~5 dynamic elements tagged
- No existing functionality broken
</success_criteria>

<output>
Create `.planning/phases/06-e2e-testing-with-cypress/06-00-SUMMARY.md` when done
</output>
