---
phase: 01-ui-redesign
plan: 03
type: execute
wave: 2
depends_on: [01-01]
files_modified:
  - Views/Home/Index.cshtml
  - wwwroot/js/index/index.js
  - wwwroot/js/utils/templates/printTemplates.js
  - wwwroot/js/utils/templates/getLatestTemplates.js
autonomous: true
requirements: [REQ-02, REQ-03, REQ-04]

must_haves:
  truths:
    - "Home page displays template cards in a responsive grid layout"
    - "Search bar has debounced input with dropdown results"
    - "Loading skeleton states appear while templates load"
    - "Empty state shows friendly message when no templates found"
    - "Template cards show title, description, topic, like count, and creator"
  artifacts:
    - path: "Views/Home/Index.cshtml"
      provides: "Redesigned home page with Tailwind"
      contains: "tailwind"
    - path: "wwwroot/js/index/index.js"
      provides: "Updated home page JS for new UI"
      contains: "skeleton"
  key_links:
    - from: "Views/Home/Index.cshtml"
      to: "/template/template"
      via: "template card click"
      pattern: "href=\"/template/template\""
    - from: "wwwroot/js/index/index.js"
      to: "/template/templates"
      via: "fetch API"
      pattern: "fetch.*template"
---

<objective>
Redesign the Home page with modern template discovery UX, including search, filtering, and responsive card grid.

Purpose: The home page is the primary landing page for template discovery. It must guide users to find, browse, and create templates with clear visual hierarchy and smooth interactions.

Output: Redesigned Home page with search, template grid, skeleton loading, empty states, and user greeting.
</objective>

<execution_context>
@/home/victor/.config/opencode/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-ui-redesign/01-CONTEXT.md
@Views/Home/Index.cshtml
@wwwroot/js/index/index.js
@wwwroot/js/utils/templates/printTemplates.js
@wwwroot/js/utils/templates/getLatestTemplates.js
@Views/Shared/_Layout.cshtml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Redesign Home Page View</name>
  <files>Views/Home/Index.cshtml</files>
  <read_first>
    - Views/Home/Index.cshtml (current implementation)
    - Views/Shared/_Layout.cshtml (new layout structure)
    - tailwind.config.js (theme values)
  </read_first>
  <action>
Replace `Views/Home/Index.cshtml` with Tailwind-styled home page:

Structure:
1. **Hero Section** (for guests):
   - `bg-gradient-to-r from-primary to-blue-700 text-white py-16 px-8 rounded-2xl mb-8`
   - Headline: "Create and Share Templates" in `text-4xl font-bold`
   - Subtitle: "Build custom forms, surveys, and questionnaires in minutes" in `text-xl text-blue-100`
   - CTA button: "Get Started" linking to /user/sign-up (guests) or "Create Template" linking to /template/create (authenticated)
   - Only shows when user is NOT logged in (check TempData["username"])

2. **User Greeting** (for authenticated users):
   - `bg-white rounded-2xl shadow-soft p-6 mb-8`
   - "Welcome back, {username}" with user avatar/initials circle
   - Quick actions: "Create Template" button, "My Templates" link
   - Only shows when user IS logged in

3. **Search Section**:
   - `relative max-w-2xl mx-auto mb-8`
   - Search input with icon: `<input class="w-full pl-12 pr-4 py-4 bg-white border border-gray-200 rounded-xl shadow-soft focus:ring-2 focus:ring-primary focus:border-transparent text-lg">`
   - Search icon (magnifying glass) positioned absolutely inside input
   - Results dropdown: `absolute w-full bg-white border border-gray-200 rounded-xl shadow-lg mt-2 max-h-96 overflow-y-auto z-50`
   - Each result: template title + topic badge + creator name
   - Debounced input (300ms delay before search)

4. **Templates Grid**:
   - Section header: "Latest Templates" with optional "Your Templates" toggle button
   - Grid container: `grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6`
   - **Template Card** (repeated for each template):
     - `bg-white rounded-2xl shadow-soft overflow-hidden hover:shadow-lg transition-shadow cursor-pointer`
     - Card image/header: colored gradient based on topic (use template topic to determine color)
     - Card body: `p-6`
       - Title: `text-xl font-semibold text-gray-900 mb-2`
       - Description: `text-gray-600 text-sm mb-4 line-clamp-2`
       - Topic badge: `inline-block px-3 py-1 bg-primary/10 text-primary text-xs font-medium rounded-full`
     - Card footer: `px-6 py-4 border-t border-gray-100 flex justify-between items-center`
       - Creator: `<span class="text-sm text-gray-500">by {username}</span>`
       - Likes: `<span class="flex items-center gap-1 text-sm text-gray-500"><svg>...</svg> {count}</span>`

5. **Skeleton Loading State** (shown while templates load):
   - Same grid layout but with animated skeleton cards
   - Each skeleton card: `bg-white rounded-2xl shadow-soft overflow-hidden`
   - Skeleton elements: `bg-gray-200 rounded animate-pulse`
   - Skeleton lines: varying widths (full, 3/4, 1/2) for realistic appearance
   - Include 6 skeleton cards in the grid

6. **Empty State** (shown when no templates found):
   - `text-center py-16`
   - Large icon (file/document icon): `text-gray-300 text-6xl mb-4`
   - Message: "No templates yet" or "No templates match your search"
   - Subtext: "Be the first to create one!" with link to /template/create

7. **Pagination** (if needed):
   - "Load More" button at bottom: `mx-auto block px-8 py-3 bg-white border border-gray-200 rounded-xl hover:bg-gray-50 transition-colors`
   - Or infinite scroll with intersection observer

Nielsen Heuristics addressed:
- #1 Visibility of system status: Skeleton loading states
- #3 User control and freedom: Clear search with clear button
- #6 Recognition rather than recall: Template cards with visual preview
- #8 Aesthetic and minimalist design: Clean grid, whitespace, clear hierarchy
  </action>
  <verify>
    <automated>grep -q "grid grid-cols" Views/Home/Index.cshtml && grep -q "skeleton" Views/Home/Index.cshtml && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - Views/Home/Index.cshtml contains "grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3"
    - Views/Home/Index.cshtml contains skeleton loading elements
    - Views/Home/Index.cshtml contains empty state component
    - Views/Home/Index.cshtml contains search input with icon
    - Views/Home/Index.cshtml contains template card structure
    - Views/Home/Index.cshtml does NOT contain "btn btn-primary" (Bootstrap)
    - Views/Home/Index.cshtml contains conditional rendering for guest vs authenticated
  </acceptance_criteria>
  <done>Home page view is redesigned with Tailwind, skeleton loading, empty states, and responsive card grid</done>
</task>

<task type="auto">
  <name>Task 2: Update Home Page JavaScript</name>
  <files>wwwroot/js/index/index.js, wwwroot/js/utils/templates/printTemplates.js</files>
  <read_first>
    - wwwroot/js/index/index.js (current implementation)
    - wwwroot/js/utils/templates/printTemplates.js (current template rendering)
    - wwwroot/js/utils/templates/getLatestTemplates.js (current API call)
    - Views/Home/Index.cshtml (just redesigned, reference new element IDs)
  </read_first>
  <action>
Update `wwwroot/js/index/index.js` to work with new UI:

1. **Template Loading:**
   - Show skeleton grid initially (remove skeletons when templates load)
   - Fetch templates from `/template/templates?page=1&limit=12`
   - On success: hide skeleton, render template cards in grid
   - On error: show error toast, hide skeleton

2. **Search Functionality:**
   - Debounce search input (300ms)
   - On input: fetch from `/template/get-by-query?text={query}`
   - Show results dropdown with template cards
   - Click on result: navigate to `/template/template?templateId={id}`
   - Empty results: show "No templates found" in dropdown
   - Click outside dropdown: close it

3. **User Templates Toggle:**
   - "Your Templates" button fetches from `/template/template/user?userId={id}`
   - Toggle between "Latest Templates" and "Your Templates" views
   - Active state styling on toggle button

4. **Infinite Scroll / Load More:**
   - Intersection observer on "Load More" button
   - When visible: fetch next page, append to grid
   - Remove button when no more results

Update `wwwroot/js/utils/templates/printTemplates.js` to:
- Generate Tailwind-styled template cards (not Bootstrap)
- Include topic badge with dynamic color based on topic
- Include like count with heart/star icon
- Include creator name
- Handle click to navigate to template view
- Generate skeleton cards for loading state

New helper function needed:
```javascript
function createSkeletonCard() {
  return `<div class="bg-white rounded-2xl shadow-soft overflow-hidden">
    <div class="h-32 bg-gray-200 animate-pulse"></div>
    <div class="p-6">
      <div class="h-6 bg-gray-200 rounded animate-pulse mb-2 w-3/4"></div>
      <div class="h-4 bg-gray-200 rounded animate-pulse mb-4 w-full"></div>
      <div class="h-4 bg-gray-200 rounded animate-pulse w-1/2"></div>
    </div>
  </div>`;
}
```
  </action>
  <verify>
    <automated>grep -q "skeleton" wwwroot/js/index/index.js && grep -q "createSkeletonCard" wwwroot/js/utils/templates/printTemplates.js && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - wwwroot/js/index/index.js contains skeleton show/hide logic
    - wwwroot/js/index/index.js contains debounced search (300ms delay)
    - wwwroot/js/index/index.js contains search results dropdown handling
    - wwwroot/js/utils/templates/printTemplates.js contains "rounded-2xl" (Tailwind card)
    - wwwroot/js/utils/templates/printTemplates.js contains "shadow-soft"
    - wwwroot/js/utils/templates/printTemplates.js contains createSkeletonCard function
    - wwwroot/js/utils/templates/printTemplates.js does NOT contain "card" (Bootstrap class)
  </acceptance_criteria>
  <done>Home page JavaScript updated with skeleton loading, debounced search, and Tailwind card rendering</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client → API | Template data fetched via fetch API |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Information Disclosure | Search queries | accept | Search is public, no sensitive data in queries |
| T-03-02 | Denial of Service | Infinite scroll | accept | Rate limiting exists on server, pagination limits results |
</threat_model>

<verification>
1. Run `dotnet build` to ensure no compilation errors
2. Navigate to home page — verify skeleton loading appears
3. Wait for templates to load — verify grid renders with cards
4. Test search: type query, verify debounced results appear
5. Test "Your Templates" toggle (as authenticated user)
6. Test empty state: search for non-existent term
7. Resize browser — verify grid is responsive (1 col mobile, 2 col tablet, 3 col desktop)
</verification>

<success_criteria>
- Skeleton loading appears immediately on page load
- Template cards render in responsive grid (1/2/3 columns)
- Search input is debounced and shows results dropdown
- Empty state displays when no templates found
- "Your Templates" toggle works for authenticated users
- Cards show title, description, topic, likes, and creator
- No Bootstrap classes remain in Home/Index.cshtml
- No JavaScript console errors
</success_criteria>

<output>
Create `.planning/phases/01-ui-redesign/01-03-SUMMARY.md` when done
</output>
