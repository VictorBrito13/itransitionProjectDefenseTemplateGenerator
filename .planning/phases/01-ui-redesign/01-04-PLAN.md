---
phase: 01-ui-redesign
plan: 04
type: execute
wave: 2
depends_on: [01-01]
files_modified:
  - Views/Template/CreateTemplate.cshtml
  - Views/Template/TemplateView.cshtml
  - wwwroot/js/createTemplate/createTemplate.js
  - wwwroot/js/templateView/templateView.js
autonomous: true
requirements: [REQ-02, REQ-03, REQ-04]

must_haves:
  truths:
    - "Template builder has intuitive question type selection with icons"
    - "Template response form has progress indication"
    - "Modals use Tailwind styling with clear action buttons"
    - "Delete confirmation uses destructive action pattern (red button, typed confirmation)"
    - "All form inputs have consistent styling and focus states"
  artifacts:
    - path: "Views/Template/CreateTemplate.cshtml"
      provides: "Redesigned template builder with Tailwind"
      contains: "tailwind"
    - path: "Views/Template/TemplateView.cshtml"
      provides: "Redesigned template response page"
      contains: "tailwind"
  key_links:
    - from: "Views/Template/CreateTemplate.cshtml"
      to: "/template/create"
      via: "form submission"
      pattern: "btn-create-template"
    - from: "Views/Template/TemplateView.cshtml"
      to: "/template/template"
      via: "template response"
      pattern: "form-questions"
---

<objective>
Redesign the Template Builder (CreateTemplate) and Template Response (TemplateView) pages with modern Tailwind styling and improved UX.

Purpose: These are the core application pages where users create and fill templates. They have the most complex UI and need the most UX attention — intuitive question building, clear form layouts, and proper feedback.

Output: Redesigned CreateTemplate and TemplateView pages with Tailwind, improved modals, and better UX patterns.
</objective>

<execution_context>
@/home/victor/.config/opencode/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-ui-redesign/01-CONTEXT.md
@Views/Template/CreateTemplate.cshtml
@Views/Template/TemplateView.cshtml
@wwwroot/js/createTemplate/createTemplate.js
@wwwroot/js/templateView/templateView.js
@Views/Shared/_Layout.cshtml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Redesign Template Builder Page</name>
  <files>Views/Template/CreateTemplate.cshtml</files>
  <read_first>
    - Views/Template/CreateTemplate.cshtml (current implementation - most complex view)
    - Views/Shared/_Layout.cshtml (new layout structure)
    - tailwind.config.js (theme values)
  </read_first>
  <action>
Replace `Views/Template/CreateTemplate.cshtml` with Tailwind-styled template builder:

Structure:
1. **Page Header**:
   - `mb-8`
   - Title: "Create a new template" in `text-3xl font-bold text-gray-900`
   - Topic selector: `<select class="px-4 py-2 border border-gray-300 rounded-lg focus:ring-2 focus:ring-primary">`
   - Create button: `<button class="px-6 py-2 bg-secondary text-white font-medium rounded-lg hover:bg-emerald-600 transition-colors">`

2. **Question Type Selector** (toolbar):
   - `bg-white rounded-2xl shadow-soft p-6 mb-6`
   - Header: "Add Questions" in `text-lg font-semibold`
   - Question type buttons in a grid: `grid grid-cols-2 md:grid-cols-3 gap-4`
   - Each button: `flex items-center gap-3 p-4 border-2 border-gray-200 rounded-xl hover:border-primary hover:bg-primary/5 transition-all cursor-pointer`
   - Button content: Icon (SVG inline) + Label
     - Single-line: text icon + "Single Line Text"
     - Multiline: align-left icon + "Paragraph"
     - Positive integer: hash icon + "Number"
     - Checkbox: check-square icon + "Checkboxes"
     - Multiple options: circle icon + "Multiple Choice"
   - Selected state: `border-primary bg-primary/5` with primary color icon

3. **Template Editor Area**:
   - `bg-white rounded-2xl shadow-soft p-8`
   - Template title: `<h2 contenteditable class="text-2xl font-bold text-gray-900 outline-none" placeholder="Template title">`
   - Template description: `<p contenteditable class="text-gray-600 mt-2 outline-none" placeholder="Add a description...">`
   - Visibility toggle: custom toggle switch (not Bootstrap form-select)
     - `<div class="flex items-center gap-3"><label class="text-sm text-gray-600">Visibility</label><button role="switch" class="w-12 h-6 bg-gray-200 rounded-full relative transition-colors"><span class="absolute left-1 top-1 w-4 h-4 bg-white rounded-full transition-transform"></span></button></div>`
     - Public = toggle right (bg-secondary), Private = toggle left (bg-gray-200)

4. **Question Cards** (dynamically added):
   - Each question: `border border-gray-200 rounded-xl p-4 mb-4 relative group`
   - Question header: question number + type badge + drag handle
   - Question input: `<input class="w-full px-4 py-2 border border-gray-200 rounded-lg focus:ring-2 focus:ring-primary" placeholder="Enter your question">`
   - Delete button: `<button class="absolute top-2 right-2 opacity-0 group-hover:opacity-100 text-gray-400 hover:text-red-500 transition-opacity">`

5. **Modals** (replace Bootstrap modals):
   - Modal overlay: `<div class="fixed inset-0 bg-black/50 z-50 flex items-center justify-center">`
   - Modal content: `<div class="bg-white rounded-2xl shadow-xl max-w-lg w-full mx-4 p-6">`
   - Modal header: title + close button (X icon)
   - Modal body: content
   - Modal footer: `<div class="flex justify-end gap-3 mt-6"><button class="px-4 py-2 text-gray-600 hover:bg-gray-100 rounded-lg">Cancel</button><button class="px-4 py-2 bg-primary text-white rounded-lg">Confirm</button></div>`
   - Apply to: admin modal, options modal, delete confirmation modal

6. **Danger Zone** (at bottom):
   - `border-2 border-red-200 rounded-2xl p-6 mt-8`
   - Header: "Danger Zone" in `text-red-600 font-semibold`
   - Delete button: `<button class="px-4 py-2 bg-red-500 text-white rounded-lg hover:bg-red-600">Delete Template</button>`
   - Confirmation modal requires typing template title to confirm (destructive action pattern)

Nielsen Heuristics addressed:
- #1 Visibility of system status: Question count indicator, save status
- #3 User control and freedom: Undo delete, drag to reorder questions
- #5 Error prevention: Required field validation before save
- #8 Aesthetic and minimalist design: Clean editor, focused on content
  </action>
  <verify>
    <automated>grep -q "grid grid-cols-2 md:grid-cols-3" Views/Template/CreateTemplate.cshtml && grep -q "contenteditable" Views/Template/CreateTemplate.cshtml && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - Views/Template/CreateTemplate.cshtml contains question type grid layout
    - Views/Template/CreateTemplate.cshtml contains contenteditable template title
    - Views/Template/CreateTemplate.cshtml contains custom toggle switch (not form-select)
    - Views/Template/CreateTemplate.cshtml contains modal overlay with backdrop
    - Views/Template/CreateTemplate.cshtml contains danger zone with border-red-200
    - Views/Template/CreateTemplate.cshtml does NOT contain "btn btn-secondary" (Bootstrap)
    - Views/Template/CreateTemplate.cshtml contains delete confirmation modal
  </acceptance_criteria>
  <done>Template builder is redesigned with intuitive question type selection, clean editor area, and proper modal patterns</done>
</task>

<task type="auto">
  <name>Task 2: Redesign Template Response Page</name>
  <files>Views/Template/TemplateView.cshtml, wwwroot/js/templateView/templateView.js</files>
  <read_first>
    - Views/Template/TemplateView.cshtml (current implementation)
    - wwwroot/js/templateView/templateView.js (current response logic)
    - Views/Template/CreateTemplate.cshtml (just redesigned, maintain consistency)
    - tailwind.config.js (theme values)
  </read_first>
  <action>
Replace `Views/Template/TemplateView.cshtml` with Tailwind-styled template response page:

Structure:
1. **Template Header**:
   - `bg-white rounded-2xl shadow-soft p-8 mb-6`
   - Title: `<h1 id="form-title" class="text-3xl font-bold text-gray-900"></h1>`
   - Description: `<p id="form-description" class="text-gray-600 mt-2"></p>`
   - Metadata row: `<div class="flex items-center gap-4 mt-4 text-sm text-gray-500">`
     - Creator name
     - Topic badge
     - Like count with star icon
   - Like button: `<button class="flex items-center gap-2 px-4 py-2 border border-gray-200 rounded-lg hover:bg-yellow-50 hover:border-yellow-300 transition-colors"><svg>star</svg><span id="likes-number">0</span></button>`

2. **Response Form**:
   - `bg-white rounded-2xl shadow-soft p-8`
   - Progress bar at top: `<div class="w-full bg-gray-200 rounded-full h-2 mb-8"><div id="progress-bar" class="bg-primary h-2 rounded-full transition-all" style="width: 0%"></div></div>`
   - Progress text: `<p class="text-sm text-gray-500 mb-4">0 of X questions answered</p>`
   - Email field (pre-filled, disabled): styled with Tailwind
   - Date field (pre-filled, disabled): styled with Tailwind
   - Dynamic question fields (loaded via JS):
     - Single-line: `<input class="w-full px-4 py-3 border border-gray-200 rounded-lg focus:ring-2 focus:ring-primary">`
     - Multiline: `<textarea class="w-full px-4 py-3 border border-gray-200 rounded-lg focus:ring-2 focus:ring-primary h-32 resize-none">`
     - Positive integer: `<input type="number" min="0" class="w-full px-4 py-3 border border-gray-200 rounded-lg focus:ring-2 focus:ring-primary">`
     - Checkboxes: `<div class="space-y-2">{options.map(opt => <label class="flex items-center gap-3 p-3 border border-gray-200 rounded-lg hover:bg-gray-50 cursor-pointer"><input type="checkbox" class="w-5 h-5 text-primary rounded"><span>{opt}</span></label>)}</div>`
     - Multiple options: `<div class="space-y-2">{options.map(opt => <label class="flex items-center gap-3 p-3 border border-gray-200 rounded-lg hover:bg-gray-50 cursor-pointer"><input type="radio" name="q{id}" class="w-5 h-5 text-primary"><span>{opt}</span></label>)}</div>`

3. **Submit Section**:
   - `<div class="mt-8 flex justify-end"><button id="btn-submit" class="px-8 py-3 bg-primary text-white font-medium rounded-lg hover:bg-blue-600 transition-colors focus:ring-2 focus:ring-primary focus:ring-offset-2">Submit Response</button></div>`

4. **Success State** (shown after submission):
   - `hidden text-center py-16`
   - Success icon: `<div class="w-16 h-16 bg-secondary/10 rounded-full flex items-center justify-center mx-auto mb-4"><svg class="w-8 h-8 text-secondary">checkmark</svg></div>`
   - Message: "Thank you for your response!" in `text-2xl font-bold text-gray-900`
   - Subtext: "Your response has been recorded" in `text-gray-600`

Update `wwwroot/js/templateView/templateView.js` to:
- Calculate and update progress bar as user answers questions
- Track answered questions count
- Validate required fields before submit
- Show loading state on submit button
- Show success state after submission
- Apply Tailwind classes to dynamically loaded question fields
  </action>
  <verify>
    <automated>grep -q "progress-bar" Views/Template/TemplateView.cshtml && grep -q "rounded-2xl" Views/Template/TemplateView.cshtml && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - Views/Template/TemplateView.cshtml contains progress bar element
    - Views/Template/TemplateView.cshtml contains success state (hidden by default)
    - Views/Template/TemplateView.cshtml contains like button with star icon
    - Views/Template/TemplateView.cshtml does NOT contain "form-control" (Bootstrap)
    - Views/Template/TemplateView.cshtml contains Tailwind-styled question inputs
    - wwwroot/js/templateView/templateView.js contains progress calculation logic
    - wwwroot/js/templateView/templateView.js contains success state display
  </acceptance_criteria>
  <done>Template response page redesigned with progress indicator, Tailwind form inputs, and success state</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Form → Server | Template responses submitted via POST |
| Editor → Server | Template data saved via PUT/POST |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Tampering | Contenteditable fields | accept | Server validates all input; contenteditable is UX only |
| T-04-02 | Information Disclosure | Template data | accept | Template data is public/private per user setting |
| T-04-03 | Denial of Service | Large templates | accept | Server limits question count; client-side pagination |
</threat_model>

<verification>
1. Run `dotnet build` to ensure no compilation errors
2. Navigate to /template/create — verify Tailwind-styled builder loads
3. Click each question type button — verify it adds a question card
4. Test modal opening/closing — verify Tailwind-styled modals
5. Navigate to /template/template?templateId=X — verify response page loads
6. Answer questions — verify progress bar updates
7. Submit form — verify success state appears
8. Resize browser — verify both pages are responsive
</verification>

<success_criteria>
- Template builder loads with Tailwind styling
- Question type buttons have icons and clear labels
- Modals use Tailwind styling (no Bootstrap modal classes)
- Delete confirmation requires explicit confirmation
- Template response page shows progress bar
- Progress bar updates as questions are answered
- Success state appears after form submission
- All form inputs have consistent Tailwind styling
- No Bootstrap classes remain in either view
- No JavaScript console errors
</success_criteria>

<output>
Create `.planning/phases/01-ui-redesign/01-04-SUMMARY.md` when done
</output>
