---
phase: 01-ui-redesign
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Views/Shared/_Layout.cshtml
  - Views/Shared/_Layout.cshtml.css
  - wwwroot/css/site.css
  - tailwind.config.js
  - package.json
autonomous: true
requirements: [REQ-01, REQ-02, REQ-03]

must_haves:
  truths:
    - "Tailwind CSS is loaded and functional in all views"
    - "Layout has responsive sidebar for authenticated users and top navbar for guests"
    - "Custom color palette and typography are applied consistently"
    - "All Bootstrap CDN references are removed from _Layout.cshtml"
  artifacts:
    - path: "tailwind.config.js"
      provides: "Tailwind configuration with custom theme"
      contains: "theme"
    - path: "Views/Shared/_Layout.cshtml"
      provides: "Main layout with Tailwind classes"
      contains: "tailwindcss"
    - path: "wwwroot/css/site.css"
      provides: "Minimal custom CSS (mostly replaced by Tailwind)"
      max_lines: 50
  key_links:
    - from: "Views/Shared/_Layout.cshtml"
      to: "tailwind.config.js"
      via: "Tailwind CDN with custom config"
      pattern: "tailwindcss"
---

<objective>
Set up Tailwind CSS and create the new responsive layout foundation for the entire application.

Purpose: This is the foundation that all subsequent UI work depends on. It establishes the design system (colors, typography, spacing), removes Bootstrap dependency, and creates the new layout structure with responsive navigation.

Output: Working Tailwind CSS integration, new _Layout.cshtml with sidebar/topbar navigation, custom theme configuration.
</objective>

<execution_context>
@/home/victor/.config/opencode/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-ui-redesign/01-CONTEXT.md
@Views/Shared/_Layout.cshtml
@Views/Shared/_Layout.cshtml.css
@wwwroot/css/site.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install Tailwind CSS and Create Configuration</name>
  <files>tailwind.config.js, package.json, wwwroot/css/tailwind.css</files>
  <read_first>
    - ItransitionTemplates.csproj (check for existing npm/node setup)
    - Views/Shared/_Layout.cshtml (current Bootstrap references to remove)
  </read_first>
  <action>
1. Create `package.json` with devDependencies: `tailwindcss` (v3.4.x), `postcss`, `autoprefixer`
2. Create `tailwind.config.js` with:
   - content: ["./Views/**/*.cshtml", "./wwwroot/js/**/*.js"]
   - theme.extend.colors: primary: "#3B82F6", secondary: "#10B981", error: "#EF4444", warning: "#F59E0B"
   - theme.extend.fontFamily: sans: ["Inter", "system-ui", "sans-serif"]
   - theme.extend.borderRadius: xl: "1rem", "2xl": "1.5rem"
   - theme.extend.boxShadow: soft: "0 2px 15px -3px rgba(0, 0, 0, 0.07), 0 10px 20px -2px rgba(0, 0, 0, 0.04)"
3. Create `postcss.config.js` with tailwindcss and autoprefixer plugins
4. Create `wwwroot/css/tailwind.css` with:
   - @tailwind base;
   - @tailwind components;
   - @tailwind utilities;
   - @import url('https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700&display=swap');
   - Custom component classes using @layer components: .btn-primary, .btn-secondary, .card, .input-field
5. Run `npm install` to install dependencies
  </action>
  <verify>
    <automated>test -f tailwind.config.js && test -f package.json && test -f wwwroot/css/tailwind.css && grep -q "tailwindcss" package.json && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - tailwind.config.js contains "content" array with Views/**/*.cshtml pattern
    - tailwind.config.js contains theme.extend.colors with primary, secondary, error, warning
    - tailwind.config.js contains theme.extend.fontFamily with Inter
    - package.json contains "tailwindcss" in devDependencies
    - wwwroot/css/tailwind.css contains "@tailwind base"
    - node_modules/tailwindcss exists after npm install
  </acceptance_criteria>
  <done>Tailwind CSS installed, configured with custom theme, and ready for use in views</done>
</task>

<task type="auto">
  <name>Task 2: Create New Layout with Responsive Navigation</name>
  <files>Views/Shared/_Layout.cshtml, Views/Shared/_Layout.cshtml.css, wwwroot/css/site.css</files>
  <read_first>
    - Views/Shared/_Layout.cshtml (current implementation to replace)
    - Views/Shared/_Layout.cshtml.css (current scoped styles)
    - wwwroot/css/site.css (current global styles)
    - tailwind.config.js (just created, reference theme values)
  </read_first>
  <action>
1. Replace `Views/Shared/_Layout.cshtml` with new Tailwind-based layout:
   - Remove Bootstrap CDN links (bootstrap.min.css, bootstrap-icons)
   - Add `<link rel="stylesheet" href="~/css/tailwind.css">` in head
   - Add Google Fonts link for Inter
   - Structure: fixed top navbar (h-16) with logo, search, auth buttons
   - Sidebar (hidden on mobile, visible on md+) with navigation links: Home, My Templates, Create Template
   - Main content area with proper padding (pt-20 for topbar, pl-0 md:pl-64 for sidebar)
   - Mobile: hamburger menu toggles sidebar as overlay
   - Use Alpine.js (CDN) for sidebar toggle state: `x-data="{ sidebarOpen: false }"`
   - Sidebar links use Tailwind classes: `flex items-center gap-3 px-4 py-3 text-gray-700 hover:bg-gray-100 rounded-lg transition-colors`
   - Active link highlighted with primary color background
   - User avatar/initials in top right with dropdown for logout
   - Search bar in top navbar: `input type="search" class="w-full max-w-md px-4 py-2 bg-gray-100 rounded-full focus:outline-none focus:ring-2 focus:ring-primary"`

2. Replace `Views/Shared/_Layout.cshtml.css` with minimal scoped styles (keep only what Tailwind can't handle):
   - Remove all Bootstrap overrides
   - Keep only CSS custom properties if needed for JS interop

3. Replace `wwwroot/css/site.css` with minimal utility styles:
   - Keep only CSS that can't be expressed in Tailwind (e.g., animation keyframes for skeleton loaders)
   - Max 30 lines
   - Add: `.skeleton { @apply bg-gray-200 rounded animate-pulse; }` for loading states

4. Update `Views/_ViewImports.cshtml` to add any needed tag helpers

5. Ensure all views still render correctly by checking that @RenderBody() is in the new layout
  </action>
  <verify>
    <automated>grep -q "tailwindcss" Views/Shared/_Layout.cshtml && grep -q "bootstrap" Views/Shared/_Layout.cshtml && echo "FAIL: bootstrap still present" || echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - Views/Shared/_Layout.cshtml contains "tailwindcss" CDN link
    - Views/Shared/_Layout.cshtml does NOT contain "bootstrap.min.css"
    - Views/Shared/_Layout.cshtml contains "Inter" font reference
    - Views/Shared/_Layout.cshtml contains sidebar navigation element
    - Views/Shared/_Layout.cshtml contains responsive hamburger button
    - Views/Shared/_Layout.cshtml contains @RenderBody()
    - wwwroot/css/site.css is under 50 lines
    - _Layout.cshtml.css contains no Bootstrap class overrides
  </acceptance_criteria>
  <done>New layout is fully Tailwind-based with responsive sidebar navigation, no Bootstrap dependencies</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CDN → Client | External CDN resources (Tailwind, fonts, Alpine.js) loaded over HTTPS |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Tampering | CDN resources | mitigate | Use SRI hashes on CDN links, prefer local copies for production |
| T-01-02 | Information Disclosure | Tailwind purge config | accept | Purge config only removes unused CSS, no sensitive data exposed |
</threat_model>

<verification>
1. Run `dotnet build` to ensure no compilation errors
2. Start the application and verify all pages load without CSS errors
3. Check browser dev tools: no 404 for CSS/JS resources
4. Verify responsive behavior: resize browser to mobile width, confirm sidebar collapses
5. Confirm all Bootstrap classes are replaced with Tailwind equivalents
</verification>

<success_criteria>
- Tailwind CSS loads correctly in browser (check network tab)
- No Bootstrap CSS or JS references remain in any view
- Layout displays correctly on desktop (sidebar visible) and mobile (hamburger menu works)
- Custom color palette is applied (primary blue buttons, etc.)
- Inter font renders correctly
- All existing pages still function (navigation works, forms submit)
</success_criteria>

<output>
Create `.planning/phases/01-ui-redesign/01-01-SUMMARY.md` when done
</output>
