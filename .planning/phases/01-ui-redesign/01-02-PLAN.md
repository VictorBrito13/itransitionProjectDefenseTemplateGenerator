---
phase: 01-ui-redesign
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - Views/User/LogInView.cshtml
  - Views/User/SignUpView.cshtml
  - wwwroot/js/signUp/signUp.js
autonomous: true
requirements: [REQ-02, REQ-04, REQ-05]

must_haves:
  truths:
    - "Login form has inline validation with real-time feedback"
    - "SignUp form validates password confirmation match"
    - "Error messages display as toast notifications, not inline alerts"
    - "Forms are fully responsive on mobile devices"
    - "Clear visual hierarchy guides user through form fields"
  artifacts:
    - path: "Views/User/LogInView.cshtml"
      provides: "Redesigned login page with Tailwind"
      contains: "tailwind"
    - path: "Views/User/SignUpView.cshtml"
      provides: "Redesigned signup page with Tailwind"
      contains: "tailwind"
  key_links:
    - from: "Views/User/LogInView.cshtml"
      to: "/user/log-in"
      via: "form action"
      pattern: "action=\"/user/log-in\""
    - from: "Views/User/SignUpView.cshtml"
      to: "/user/sign-up"
      via: "form action"
      pattern: "action=\"/user/sign-up\""
---

<objective>
Redesign the Login and SignUp pages with modern Tailwind styling, inline validation, and improved UX following Nielsen Heuristics.

Purpose: Authentication pages are the first impression for new users. They must be clean, trustworthy, and guide users through the process with clear feedback.

Output: Redesigned Login and SignUp views with inline validation, toast notifications, and responsive layouts.
</objective>

<execution_context>
@/home/victor/.config/opencode/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-ui-redesign/01-CONTEXT.md
@Views/User/LogInView.cshtml
@Views/User/SignUpView.cshtml
@wwwroot/js/signUp/signUp.js
@Views/Shared/_Layout.cshtml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Redesign Login Page</name>
  <files>Views/User/LogInView.cshtml</files>
  <read_first>
    - Views/User/LogInView.cshtml (current implementation)
    - Views/Shared/_Layout.cshtml (new layout structure to integrate with)
    - tailwind.config.js (available theme values)
  </read_first>
  <action>
Replace `Views/User/LogInView.cshtml` with Tailwind-styled login page:

Structure:
- Centered card layout: `min-h-screen flex items-center justify-center bg-gray-50`
- Card container: `w-full max-w-md bg-white rounded-2xl shadow-soft p-8`
- Header: Logo/brand name + "Sign in to your account" subtitle
- Form fields with labels above inputs:
  - Email: `<label class="block text-sm font-medium text-gray-700 mb-1">Email</label>`
  - Input: `<input type="email" class="w-full px-4 py-3 border border-gray-300 rounded-lg focus:ring-2 focus:ring-primary focus:border-transparent transition-all" required>`
  - Password: Same pattern with show/hide toggle button (eye icon)
- Error display: `<div class="hidden mt-3 p-3 bg-red-50 border border-red-200 rounded-lg text-red-700 text-sm" id="error-container"></div>`
- Submit button: `<button type="submit" class="w-full py-3 bg-primary text-white font-medium rounded-lg hover:bg-blue-600 transition-colors focus:ring-2 focus:ring-primary focus:ring-offset-2">Sign In</button>`
- Footer link: "Don't have an account? Sign up" linking to /user/sign-up
- Loading state on submit: button shows spinner, disabled during submission

Nielsen Heuristics addressed:
- #1 Visibility of system status: Loading spinner on submit
- #5 Error prevention: Required field indicators, email format validation
- #9 Help users recognize errors: Inline error messages with red border
- #10 Help and documentation: "Sign up" link for new users
  </action>
  <verify>
    <automated>grep -q "tailwind" Views/User/LogInView.cshtml && grep -q "form-control" Views/User/LogInView.cshtml && echo "FAIL: bootstrap still present" || echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - Views/User/LogInView.cshtml contains "max-w-md" (centered card)
    - Views/User/LogInView.cshtml contains "focus:ring-2" (focus states)
    - Views/User/LogInView.cshtml does NOT contain "form-control" (Bootstrap class)
    - Views/User/LogInView.cshtml contains error display container
    - Views/User/LogInView.cshtml contains loading state indicator
    - Views/User/LogInView.cshtml contains link to /user/sign-up
  </acceptance_criteria>
  <done>Login page is fully redesigned with Tailwind, inline validation, and responsive layout</done>
</task>

<task type="auto">
  <name>Task 2: Redesign SignUp Page</name>
  <files>Views/User/SignUpView.cshtml, wwwroot/js/signUp/signUp.js</files>
  <read_first>
    - Views/User/SignUpView.cshtml (current implementation)
    - wwwroot/js/signUp/signUp.js (current validation logic)
    - Views/User/LogInView.cshtml (just redesigned, maintain consistency)
    - tailwind.config.js (available theme values)
  </read_first>
  <action>
Replace `Views/User/SignUpView.cshtml` with Tailwind-styled signup page:

Structure:
- Same centered card layout as Login page for consistency
- Card: `w-full max-w-md bg-white rounded-2xl shadow-soft p-8`
- Header: "Create your account" + subtitle
- Form fields (all with labels above):
  - Email: `<input type="email" class="w-full px-4 py-3 border border-gray-300 rounded-lg focus:ring-2 focus:ring-primary focus:border-transparent" required>`
  - Username: Same pattern
  - Password: With strength indicator bar below (green/yellow/red based on length)
  - Confirm Password: With match indicator (green checkmark or red X)
- Error container (same pattern as Login)
- Submit button: "Create Account" with loading spinner
- Footer link: "Already have an account? Sign in" linking to /user/log-in
- Password requirements list: "At least 8 characters" with checkmark when met

Update `wwwroot/js/signUp/signUp.js` to:
- Add real-time password strength indicator (updates on input)
- Add password match validation (checks confirm field on blur)
- Show inline errors below each field (not just at top)
- Disable submit until all validations pass
- Add loading state: change button text to "Creating account..." and show spinner
- Use Tailwind classes for error states: add "border-red-500" class to invalid inputs
  </action>
  <verify>
    <automated>grep -q "tailwind" Views/User/SignUpView.cshtml && grep -q "password-strength" wwwroot/js/signUp/signUp.js && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - Views/User/SignUpView.cshtml contains "max-w-md" (centered card)
    - Views/User/SignUpView.cshtml contains password strength indicator element
    - Views/User/SignUpView.cshtml contains password match indicator element
    - Views/User/SignUpView.cshtml does NOT contain "form-control"
    - Views/User/SignUpView.cshtml contains link to /user/log-in
    - wwwroot/js/signUp/signUp.js contains "password-strength" function
    - wwwroot/js/signUp/signUp.js contains password match validation
    - wwwroot/js/signUp/signUp.js adds "border-red-500" class on validation failure
  </acceptance_criteria>
  <done>SignUp page is fully redesigned with Tailwind, real-time password validation, and consistent styling with Login page</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Form → Server | User credentials submitted via POST |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Tampering | Client-side validation | accept | Validation is UX only; server-side validation remains unchanged |
| T-02-02 | Information Disclosure | Error messages | accept | Error messages are generic, no sensitive info exposed |
</threat_model>

<verification>
1. Run `dotnet build` to ensure no compilation errors
2. Navigate to /user/log-in — verify Tailwind-styled form renders
3. Submit empty form — verify inline validation messages appear
4. Navigate to /user/sign-up — verify Tailwind-styled form renders
5. Test password strength indicator (type short vs long password)
6. Test password match indicator (type mismatched passwords)
7. Resize browser to mobile — verify forms are responsive
</verification>

<success_criteria>
- Login page renders with Tailwind styling (no Bootstrap classes)
- SignUp page renders with Tailwind styling (no Bootstrap classes)
- Password strength indicator updates in real-time
- Password match indicator shows green/red based on match
- Error messages appear below each field, not just at top
- Forms are centered and responsive on all screen sizes
- Loading state appears on form submission
- No JavaScript console errors
</success_criteria>

<output>
Create `.planning/phases/01-ui-redesign/01-02-SUMMARY.md` when done
</output>
