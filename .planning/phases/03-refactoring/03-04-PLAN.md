---
phase: 03-refactoring
plan: 04
type: execute
wave: 3
depends_on: ["03-03"]
files_modified:
  - tests/ItransitionTemplates.Tests.Integration/ItransitionTemplates.Tests.Integration.csproj
  - tests/ItransitionTemplates.Tests.Integration/CustomWebApplicationFactory.cs
  - tests/ItransitionTemplates.Tests.Integration/Controllers/HomeControllerTests.cs
  - tests/ItransitionTemplates.Tests.Integration/Controllers/TemplateControllerTests.cs
  - tests/ItransitionTemplates.Tests.Integration/Controllers/UserControllerTests.cs
  - tests/ItransitionTemplates.Tests.Integration/Controllers/QuestionControllerTests.cs
  - tests/ItransitionTemplates.Tests.Integration/Controllers/ResponseControllerTests.cs
  - ItransitionTemplates.sln
autonomous: true
requirements: [REQ-13]

must_haves:
  truths:
    - "An integration test project exists under tests/integration"
    - "Integration tests use WebApplicationFactory to spin up the app with an in-memory database"
    - "Tests cover all controller endpoints (GET, POST, PUT, DELETE)"
    - "Tests verify HTTP status codes and response shapes"
    - "All integration tests pass with dotnet test"
  artifacts:
    - path: "tests/ItransitionTemplates.Tests.Integration/ItransitionTemplates.Tests.Integration.csproj"
      provides: "Integration test project"
      contains: "Microsoft.AspNetCore.Mvc.Testing"
    - path: "tests/ItransitionTemplates.Tests.Integration/CustomWebApplicationFactory.cs"
      provides: "WebApplicationFactory configured with in-memory DB"
      contains: "CustomWebApplicationFactory"
  key_links:
    - from: "tests/ItransitionTemplates.Tests.Integration/Controllers/*.cs"
      to: "Controllers/*.cs"
      via: "HttpClient from WebApplicationFactory"
      pattern: "_client\\.(Get|Post|Put|Delete)Async"
---

<objective>
Set up integration test infrastructure using WebApplicationFactory and write integration tests for all controller endpoints.

Purpose: Integration tests verify the full request pipeline (routing, model binding, serialization, database interaction) end-to-end. They complement unit tests by catching issues in the wiring between layers.
Output: An integration test project with tests covering all API endpoints, all passing.
</objective>

<execution_context>
@/home/victor/.config/opencode/gsd-core/workflows/execute-plan.md
@/home/victor/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@Controllers/Templates.cs
@Controllers/HomeController.cs
@Controllers/UserController.cs
@Controllers/Question.cs
@Controllers/ResponseController.cs
@Program.cs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create integration test project with CustomWebApplicationFactory</name>
  <files>tests/ItransitionTemplates.Tests.Integration/ItransitionTemplates.Tests.Integration.csproj, tests/ItransitionTemplates.Tests.Integration/CustomWebApplicationFactory.cs, ItransitionTemplates.sln</files>
  <action>
    1. Create the directory: `tests/ItransitionTemplates.Tests.Integration/`

    2. Create `tests/ItransitionTemplates.Tests.Integration/ItransitionTemplates.Tests.Integration.csproj` with:
       - TargetFramework: net8.0
       - Package references: `xunit`, `xunit.runner.visualstudio`, `Microsoft.NET.Test.Sdk`, `Microsoft.AspNetCore.Mvc.Testing` (8.0.x), `Microsoft.EntityFrameworkCore.InMemory` (8.0.x)
       - Project reference to `../../ItransitionTemplates.csproj`

    3. Add to solution: `dotnet sln add tests/ItransitionTemplates.Tests.Integration/ItransitionTemplates.Tests.Integration.csproj`

    4. Create `CustomWebApplicationFactory.cs`:
       - Extend `WebApplicationFactory<Program>` (note: Program.cs uses top-level statements, so the `Program` class needs to be made accessible — add `public partial class Program { }` at the end of Program.cs if not already present)
       - Override `ConfigureWebHost` to:
         - Replace the MySQL DbContext with InMemory provider using a unique database name per test class
         - Ensure session middleware is configured (needed for auth tests)
       - Provide a `CreateAuthenticatedClient()` helper method that:
         - Creates a user in the in-memory DB
         - Makes a POST to /user/log-in to establish a session
         - Returns an HttpClient with the session cookie

    5. Add `public partial class Program { }` at the end of `Program.cs` if not present (required for WebApplicationFactory to reference the entry point class).

    6. Verify: `dotnet build tests/ItransitionTemplates.Tests.Integration/`
  </action>
  <verify>
    <automated>dotnet build tests/ItransitionTemplates.Tests.Integration/ 2>&1 | tail -5</automated>
  </verify>
  <done>
    - Integration test project exists and builds
    - CustomWebApplicationFactory replaces MySQL with InMemory DB
    - Program.cs has `public partial class Program { }` at the end
    - Project is added to the solution
  </done>
</task>

<task type="auto">
  <name>Task 2: Write integration tests for all controller endpoints</name>
  <files>tests/ItransitionTemplates.Tests.Integration/Controllers/HomeControllerTests.cs, tests/ItransitionTemplates.Tests.Integration/Controllers/TemplateControllerTests.cs, tests/ItransitionTemplates.Tests.Integration/Controllers/UserControllerTests.cs, tests/ItransitionTemplates.Tests.Integration/Controllers/QuestionControllerTests.cs, tests/ItransitionTemplates.Tests.Integration/Controllers/ResponseControllerTests.cs</files>
  <action>
    Write integration tests using the HttpClient from CustomWebApplicationFactory. Each test should verify HTTP status code and response body shape.

    1. `HomeControllerTests.cs`:
       - `GetIndex_ReturnsOkWithHtml` — GET / → 200, content-type text/html
       - `GetError_ReturnsOkWithHtml` — GET /Home/Error → 200

    2. `TemplateControllerTests.cs` (most endpoints):
       - `GetTemplates_ReturnsOkWithArray` — GET /template/templates?page=0&limit=10 → 200, JSON with data array
       - `GetTemplate_ExistingId_ReturnsTemplate` — seed template, GET /template/get-template?templateId=X → 200, JSON with template data
       - `GetTemplate_NonExistentId_ReturnsErrorMsg` — GET /template/get-template?templateId=999 → JSON with errorMsg
       - `CreateTemplate_Authenticated_ReturnsOk` — POST /template/create with valid body → 200, returns saved template
       - `CreateTemplate_InvalidTopic_ReturnsError` — POST with topicId=0 → error
       - `UpdateTemplate_Existing_ReturnsOk` — PUT /template/update?templateId=X → 200
       - `UpdateTemplate_NonExistent_ReturnsNotFound` — PUT with bad ID → 404
       - `DeleteTemplate_Existing_ReturnsOk` — DELETE /template/delete?templateId=X → 200
       - `LikeAction_Authenticated_ReturnsOk` — GET /template/like?userId=X&templateId=Y&action=like → 200
       - `GetTemplateLikes_Existing_ReturnsOk` — GET /template/likes?templateId=X → 200
       - `GetTemplatesByQuery_MatchingText_ReturnsOk` — GET /template/get-by-query?text=test → 200
       - `GetTemplatesByQuery_NoMatch_ReturnsNotFound` — GET /template/get-by-query?text=zzzzz → 404

    3. `UserControllerTests.cs`:
       - `GetLogInView_ReturnsOkWithHtml` — GET /user/log-in → 200, HTML
       - `GetSignUpView_ReturnsOkWithHtml` — GET /user/sign-up → 200, HTML
       - `SignUp_ValidUser_RedirectsToLogin` — POST /user/sign-up with form data → 302 redirect
       - `LogIn_ValidUser_RedirectsToHome` — seed user, POST /user/log-in → 302 redirect
       - `LogIn_InvalidUser_ReturnsViewWithError` — POST with bad credentials → 200 with error
       - `GetUserByUsername_Existing_ReturnsOk` — seed user, GET /user/get-by-username?username=X → 200
       - `GetUserByUsername_NonExistent_ReturnsNotFound` — GET with unknown username → 404
       - `LogOut_RedirectsToLogin` — GET /user/log-out → 302

    4. `QuestionControllerTests.cs`:
       - `AddQuestions_ValidPayload_ReturnsOk` — POST /question/add with questions+options → 200
       - `AddQuestions_EmptyQuestions_ReturnsBadRequest` — POST with empty questions → 400

    5. `ResponseControllerTests.cs`:
       - `SaveResponses_ValidPayload_ReturnsOk` — POST /response/add with responses → 200
       - `SaveResponses_InvalidPayload_ReturnsBadRequest` — POST with bad data → 400

    For tests requiring authentication, use the CustomWebApplicationFactory's session handling. For POST/PUT/DELETE tests, seed required data (users, templates, topics) directly into the in-memory DbContext before making the request.
  </action>
  <verify>
    <automated>dotnet test tests/ItransitionTemplates.Tests.Integration/ --verbosity normal 2>&1 | tail -20</automated>
  </verify>
  <done>
    - All 5 test files exist under tests/ItransitionTemplates.Tests.Integration/Controllers/
    - At least 25 integration tests total
    - All tests pass with `dotnet test`
    - Tests cover GET, POST, PUT, DELETE endpoints across all controllers
    - Both success and error paths are tested
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test HTTP client → App pipeline | Simulates real HTTP requests |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-07 | Information Disclosure | Test database | accept | In-memory DB with synthetic data, no PII, destroyed after tests |
</threat_model>

<verification>
- `dotnet test tests/ItransitionTemplates.Tests.Integration/` passes with zero failures
- At least 25 integration tests exist
- All controller endpoints have at least one test
- Both authenticated and unauthenticated paths tested where applicable
</verification>

<success_criteria>
- Integration test project created with WebApplicationFactory
- CustomWebApplicationFactory configured with in-memory DB
- Integration tests cover all 5 controllers
- All tests pass
- Combined with unit tests (Plan 03), full test coverage of business logic and API surface
</success_criteria>

<output>
Create `.planning/phases/03-refactoring/03-04-SUMMARY.md` when done
</output>
