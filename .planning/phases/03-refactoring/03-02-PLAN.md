---
phase: 03-refactoring
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - wwwroot/js/createTemplate/Single-string-question.js
  - wwwroot/js/createTemplate/Multiline-question.js
  - wwwroot/js/createTemplate/Positive-integer-question.js
  - wwwroot/js/createTemplate/Checkbox-question.js
  - wwwroot/js/createTemplate/Multiple-options-question.js
  - wwwroot/js/createTemplate/BaseQuestion.js
  - wwwroot/js/utils/validation.js
  - wwwroot/js/utils/toast.js
  - wwwroot/js/index/index.js
  - wwwroot/js/templateView/templateView.js
  - wwwroot/js/createTemplate/createTemplate.js
  - wwwroot/js/signUp/signUp.js
  - wwwroot/js/updateTemplate/updateTemplate.js
  - wwwroot/js/site.js
  - Views/Home/Index.cshtml
  - Views/Shared/_ToastPartial.cshtml
  - Views/Template/CreateTemplate.cshtml
  - Views/Template/TemplateView.cshtml
  - Views/User/LogInView.cshtml
  - Views/User/SignUpView.cshtml
autonomous: true
requirements: [REQ-10, REQ-11, REQ-12]

must_haves:
  truths:
    - "All 5 question type classes extend a shared BaseQuestion class with no duplicated constructor/delete/label logic"
    - "showFieldError and clearFieldError exist in a single shared utility file, not duplicated across views"
    - "TempData toast notification pattern is extracted into a reusable Razor partial"
    - "Skeleton cards in Index.cshtml are generated via a loop, not 6 hardcoded HTML blocks"
    - "Unused JS files (updateTemplate.js, site.js placeholder) are removed"
  artifacts:
    - path: "wwwroot/js/createTemplate/BaseQuestion.js"
      provides: "Base class for all question types"
      contains: "class BaseQuestion"
    - path: "wwwroot/js/utils/validation.js"
      provides: "Shared form validation utilities"
      contains: "showFieldError"
    - path: "Views/Shared/_ToastPartial.cshtml"
      provides: "Reusable toast notification from TempData"
      contains: "showToast"
  key_links:
    - from: "wwwroot/js/createTemplate/*-question.js"
      to: "wwwroot/js/createTemplate/BaseQuestion.js"
      via: "class extension"
      pattern: "extends BaseQuestion"
    - from: "Views/User/*.cshtml"
      to: "wwwroot/js/utils/validation.js"
      via: "import"
      pattern: "import.*validation"
---

<objective>
Refactor all frontend JavaScript to eliminate duplicated patterns across question type classes, form validation, and toast notifications. Remove unused files and consolidate skeleton card markup.

Purpose: The 5 question type classes share ~80% identical code (constructor, delete button, label editing, container structure). Form validation functions are duplicated in LogInView and SignUpView. Toast TempData blocks are copy-pasted in 4 views. Skeleton cards are hardcoded 6 times in Index.cshtml.
Output: A BaseQuestion class, shared validation utility, toast partial view, and cleaner markup.
</objective>

<execution_context>
@/home/victor/.config/opencode/gsd-core/workflows/execute-plan.md
@/home/victor/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@wwwroot/js/createTemplate/Single-string-question.js
@wwwroot/js/createTemplate/Multiline-question.js
@wwwroot/js/createTemplate/Positive-integer-question.js
@wwwroot/js/createTemplate/Checkbox-question.js
@wwwroot/js/createTemplate/Multiple-options-question.js
@wwwroot/js/signUp/signUp.js
@Views/User/LogInView.cshtml
@Views/Home/Index.cshtml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create BaseQuestion class and refactor all 5 question types</name>
  <files>wwwroot/js/createTemplate/BaseQuestion.js, wwwroot/js/createTemplate/Single-string-question.js, wwwroot/js/createTemplate/Multiline-question.js, wwwroot/js/createTemplate/Positive-integer-question.js, wwwroot/js/createTemplate/Checkbox-question.js, wwwroot/js/createTemplate/Multiple-options-question.js</files>
  <action>
    1. Create `wwwroot/js/createTemplate/BaseQuestion.js` with a base class that encapsulates all shared logic:
       - Constructor accepting `(label, editionMode, questionId)` with defaults (`label = "Add a label"`, `editionMode = true`)
       - `createContainer()` method — creates the div with `className = "mt-4"` and sets `dataset["QuestionType"]`
       - `createLabel()` method — creates label element with text content
       - `addEditionControls(container, label)` method — adds contentEditable to label, creates delete button with `deleteElementOnClick`, appends to container
       - Abstract-like `createInput()` method — to be overridden by subclasses (returns the specific input element)
       - `getQuestionHTML()` method — orchestrates: createContainer → createLabel → createInput → addEditionControls (if editionMode) → append input → return container

    2. Refactor each question class to extend BaseQuestion:
       - `Single-string-question.js` → `export default class SingleLineQuestion extends BaseQuestion` — only override `createInput()` to return `<input>` with default type/text styling
       - `Multiline-question.js` → override `createInput()` to return `<textarea>` with same styling
       - `Positive-integer-question.js` → override `createInput()` to return `<input type="number" min="0">`
       - `Checkbox-question.js` → override `createInput()` to return `<input type="checkbox">`. ALSO fix Bootstrap remnants: replace `d-flex gap-3 align-items-center` with Tailwind, replace `form-check-input`/`form-check-label` with Tailwind equivalents
       - `Multiple-options-question.js` → override `createInput()` to return `<select>` with options. Keep the unique `#selectId` and `getId()` method. Keep the edit options button logic (it's unique to this type)

    3. Verify that `insertQuestions.js` still works without changes (it imports and instantiates the same class names).
  </action>
  <verify>
    <automated>grep -c "extends BaseQuestion" wwwroot/js/createTemplate/*-question.js</automated>
  </verify>
  <done>
    - BaseQuestion.js exists with shared constructor, container creation, label creation, edition controls
    - All 5 question classes extend BaseQuestion
    - Each subclass only contains its unique input creation logic
    - Checkbox-question.js has no Bootstrap class remnants (no d-flex, form-check-input, form-check-label)
    - insertQuestions.js still imports and works with the refactored classes
  </done>
</task>

<task type="auto">
  <name>Task 2: Extract shared validation utility and toast partial, remove unused files</name>
  <files>wwwroot/js/utils/validation.js, Views/Shared/_ToastPartial.cshtml, Views/User/LogInView.cshtml, Views/User/SignUpView.cshtml, Views/Template/CreateTemplate.cshtml, Views/Template/TemplateView.cshtml, wwwroot/js/signUp/signUp.js, wwwroot/js/updateTemplate/updateTemplate.js, wwwroot/js/site.js, wwwroot/js/index/index.js</files>
  <action>
    1. Create `wwwroot/js/utils/validation.js` with shared validation functions:
       - `showFieldError(input, errorEl, message)` — adds border-red-500, removes border-gray-300, sets error text, shows error element
       - `clearFieldError(input, errorEl)` — removes border-red-500, adds border-gray-300, hides error element
       - Export both as named exports

    2. Create `Views/Shared/_ToastPartial.cshtml` — a reusable Razor partial that reads TempData and emits showToast calls:
       - Check `TempData["errorMsg"]` → emit `showToast('error', '...')`
       - Check `TempData["successMsg"]` → emit `showToast('success', '...')`
       - This replaces the identical inline script blocks in CreateTemplate.cshtml, TemplateView.cshtml, LogInView.cshtml, and SignUpView.cshtml

    3. Update all 4 views to use the partial:
       - Replace the inline `<script>...showToast...</script>` blocks with `@await Html.PartialAsync("_ToastPartial")` or `@{ await Html.RenderPartialAsync("_ToastPartial"); }`
       - In LogInView.cshtml: Remove the inline `showFieldError` and `clearFieldError` function definitions (lines 140-151). Instead, import from validation.js. Since LogInView uses an inline IIFE script, add `<script type="module">` that imports from validation.js and uses those functions.
       - In SignUpView.cshtml / signUp.js: Remove the duplicate `showFieldError` and `clearFieldError` definitions (lines 37-48). Import from validation.js instead.

    4. Remove unused files:
       - Delete `wwwroot/js/updateTemplate/updateTemplate.js` — contains only an empty exported function, never imported anywhere
       - Delete `wwwroot/js/site.js` — contains only a comment, no actual code. Remove the `<script src="~/js/site.js">` reference from `_Layout.cshtml`.

    5. Refactor `Views/Home/Index.cshtml` skeleton cards (lines 83-154): Replace the 6 hardcoded identical skeleton card divs with a single skeleton card inside a Razor `@for` loop (e.g., `@for(int i = 0; i < 6; i++) { ... }`).

    6. In `wwwroot/js/index/index.js`: The toggle templates handler (lines 134-186) has nearly identical try/catch blocks for loading latest vs user templates. Extract a shared `loadAndRenderTemplates(fetchFn, mode)` helper function that handles: showSkeletons → fetch → hideSkeletons → show/hide empty state → printTemplates.
  </action>
  <verify>
    <automated>test ! -f wwwroot/js/updateTemplate/updateTemplate.js && test ! -f wwwroot/js/site.js && echo "Unused files removed" || echo "Files still exist"</automated>
  </verify>
  <done>
    - `wwwroot/js/utils/validation.js` exists with showFieldError and clearFieldError
    - `Views/Shared/_ToastPartial.cshtml` exists and handles errorMsg/successMsg TempData
    - All 4 views use the toast partial instead of inline script blocks
    - LogInView and signUp.js import validation functions from shared utility
    - `updateTemplate.js` and `site.js` deleted, layout reference removed
    - Index.cshtml skeleton cards use a loop instead of 6 hardcoded blocks
    - index.js has a shared loadAndRenderTemplates helper
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client JS → Server API | User input sent via fetch calls |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-04 | Tampering | Client-side validation | accept | Client validation is UX-only; server-side validation in controllers is the security boundary |
| T-03-05 | Information Disclosure | Toast messages | accept | Toast messages show same info as previous TempData approach, no new exposure |
</threat_model>

<verification>
- All 5 question classes extend BaseQuestion (grep confirms)
- `grep -r "showFieldError" wwwroot/js/signUp/ Views/User/` shows imports from validation.js, not local definitions
- `Views/Shared/_ToastPartial.cshtml` exists
- `test ! -f wwwroot/js/updateTemplate/updateTemplate.js` passes
- `test ! -f wwwroot/js/site.js` passes
- Index.cshtml has no more than 1 skeleton card HTML block (rest generated by loop)
</verification>

<success_criteria>
- BaseQuestion class created, all 5 question types refactored to extend it
- Shared validation utility created and consumed by LogInView and SignUpView
- Toast partial view created and used by all 4 views
- 2 unused JS files removed
- Skeleton card duplication eliminated
- index.js toggle handler deduplicated
</success_criteria>

<output>
Create `.planning/phases/03-refactoring/03-02-SUMMARY.md` when done
</output>
